// Generated by `bun run codegen` in apps/ios. Don't edit it.

public enum SerializedNode: Equatable, Sendable {
  case article(SerializedArticleNode)
  case autocomplete(SerializedAutocompleteNode)
  case autoLink(SerializedAutoLinkNode)
  case callout(SerializedCalloutNode)
  case chart(SerializedChartNode)
  case documentCode(SerializedDocumentCodeNode)
  case codeHighlight(SerializedCodeHighlightNode)
  case collapsibleContainer(SerializedCollapsibleContainerNode)
  case collapsibleContent(SerializedCollapsibleContentNode)
  case collapsibleTitle(SerializedCollapsibleTitleNode)
  case comment(SerializedCommentNode)
  case emoji(SerializedEmojiNode)
  case equation(SerializedEquationNode)
  case excalidraw(SerializedExcalidrawNode)
  case figma(SerializedFigmaNode)
  case footnoteDefinition(SerializedFootnoteDefinitionNode)
  case footnoteReference(SerializedFootnoteReferenceNode)
  case hashtag(SerializedHashtagNode)
  case heading(SerializedHeadingNode)
  case horizontalRule(SerializedHorizontalRuleNode)
  case image(SerializedImageNode)
  case inlineImage(SerializedInlineImageNode)
  case keyword(SerializedKeywordNode)
  case layoutContainer(SerializedLayoutContainerNode)
  case layoutItem(SerializedLayoutItemNode)
  case lineBreak(SerializedLineBreakNode)
  case link(SerializedLinkNode)
  case list(SerializedListNode)
  case listItem(SerializedListItemNode)
  case mark(SerializedMarkNode)
  case mention(SerializedMentionNode)
  case mermaid(SerializedMermaidNode)
  case pageBreak(SerializedPageBreakNode)
  case paragraph(SerializedParagraphNode)
  case poll(SerializedPollNode)
  case quote(SerializedQuoteNode)
  case root(SerializedRootNode)
  case slide(SerializedSlideNode)
  case sticky(SerializedStickyNode)
  case tab(SerializedTabNode)
  case table(SerializedTableNode)
  case tableCell(SerializedTableCellNode)
  case tableRow(SerializedTableRowNode)
  case text(SerializedTextNode)
  case thread(SerializedThreadNode)
  case tweet(SerializedTweetNode)
  case video(SerializedVideoNode)
  case youTube(SerializedYouTubeNode)
  /// A node whose type isn't declared, kept exactly as read.
  case opaque(JSONValue)

  public init(json: JSONValue) {
    switch json["type"]?.stringValue {
    case "article": self.init(json, as: Self.article)
    case "autocomplete": self.init(json, as: Self.autocomplete)
    case "autolink": self.init(json, as: Self.autoLink)
    case "callout": self.init(json, as: Self.callout)
    case "chart": self.init(json, as: Self.chart)
    case "code": self.init(json, as: Self.documentCode)
    case "code-highlight": self.init(json, as: Self.codeHighlight)
    case "collapsible-container": self.init(json, as: Self.collapsibleContainer)
    case "collapsible-content": self.init(json, as: Self.collapsibleContent)
    case "collapsible-title": self.init(json, as: Self.collapsibleTitle)
    case "comment": self.init(json, as: Self.comment)
    case "emoji": self.init(json, as: Self.emoji)
    case "equation": self.init(json, as: Self.equation)
    case "excalidraw": self.init(json, as: Self.excalidraw)
    case "figma": self.init(json, as: Self.figma)
    case "footnote-definition": self.init(json, as: Self.footnoteDefinition)
    case "footnote-reference": self.init(json, as: Self.footnoteReference)
    case "hashtag": self.init(json, as: Self.hashtag)
    case "heading": self.init(json, as: Self.heading)
    case "horizontalrule": self.init(json, as: Self.horizontalRule)
    case "image": self.init(json, as: Self.image)
    case "inline-image": self.init(json, as: Self.inlineImage)
    case "keyword": self.init(json, as: Self.keyword)
    case "layout-container": self.init(json, as: Self.layoutContainer)
    case "layout-item": self.init(json, as: Self.layoutItem)
    case "linebreak": self.init(json, as: Self.lineBreak)
    case "link": self.init(json, as: Self.link)
    case "list": self.init(json, as: Self.list)
    case "listitem": self.init(json, as: Self.listItem)
    case "mark": self.init(json, as: Self.mark)
    case "mention": self.init(json, as: Self.mention)
    case "mermaid": self.init(json, as: Self.mermaid)
    case "page-break": self.init(json, as: Self.pageBreak)
    case "paragraph": self.init(json, as: Self.paragraph)
    case "poll": self.init(json, as: Self.poll)
    case "quote": self.init(json, as: Self.quote)
    case "root": self.init(json, as: Self.root)
    case "slide-deck": self.init(json, as: Self.slide)
    case "sticky": self.init(json, as: Self.sticky)
    case "tab": self.init(json, as: Self.tab)
    case "table": self.init(json, as: Self.table)
    case "tablecell": self.init(json, as: Self.tableCell)
    case "tablerow": self.init(json, as: Self.tableRow)
    case "text": self.init(json, as: Self.text)
    case "thread": self.init(json, as: Self.thread)
    case "tweet": self.init(json, as: Self.tweet)
    case "video": self.init(json, as: Self.video)
    case "youtube": self.init(json, as: Self.youTube)
    default: self = .opaque(json)
    }
  }

  public var payload: (any NodePayload)? {
    switch self {
    case .article(let node): node
    case .autocomplete(let node): node
    case .autoLink(let node): node
    case .callout(let node): node
    case .chart(let node): node
    case .documentCode(let node): node
    case .codeHighlight(let node): node
    case .collapsibleContainer(let node): node
    case .collapsibleContent(let node): node
    case .collapsibleTitle(let node): node
    case .comment(let node): node
    case .emoji(let node): node
    case .equation(let node): node
    case .excalidraw(let node): node
    case .figma(let node): node
    case .footnoteDefinition(let node): node
    case .footnoteReference(let node): node
    case .hashtag(let node): node
    case .heading(let node): node
    case .horizontalRule(let node): node
    case .image(let node): node
    case .inlineImage(let node): node
    case .keyword(let node): node
    case .layoutContainer(let node): node
    case .layoutItem(let node): node
    case .lineBreak(let node): node
    case .link(let node): node
    case .list(let node): node
    case .listItem(let node): node
    case .mark(let node): node
    case .mention(let node): node
    case .mermaid(let node): node
    case .pageBreak(let node): node
    case .paragraph(let node): node
    case .poll(let node): node
    case .quote(let node): node
    case .root(let node): node
    case .slide(let node): node
    case .sticky(let node): node
    case .tab(let node): node
    case .table(let node): node
    case .tableCell(let node): node
    case .tableRow(let node): node
    case .text(let node): node
    case .thread(let node): node
    case .tweet(let node): node
    case .video(let node): node
    case .youTube(let node): node
    case .opaque: nil
    }
  }

  public var json: JSONValue {
    switch self {
    case .article(let node): node.json
    case .autocomplete(let node): node.json
    case .autoLink(let node): node.json
    case .callout(let node): node.json
    case .chart(let node): node.json
    case .documentCode(let node): node.json
    case .codeHighlight(let node): node.json
    case .collapsibleContainer(let node): node.json
    case .collapsibleContent(let node): node.json
    case .collapsibleTitle(let node): node.json
    case .comment(let node): node.json
    case .emoji(let node): node.json
    case .equation(let node): node.json
    case .excalidraw(let node): node.json
    case .figma(let node): node.json
    case .footnoteDefinition(let node): node.json
    case .footnoteReference(let node): node.json
    case .hashtag(let node): node.json
    case .heading(let node): node.json
    case .horizontalRule(let node): node.json
    case .image(let node): node.json
    case .inlineImage(let node): node.json
    case .keyword(let node): node.json
    case .layoutContainer(let node): node.json
    case .layoutItem(let node): node.json
    case .lineBreak(let node): node.json
    case .link(let node): node.json
    case .list(let node): node.json
    case .listItem(let node): node.json
    case .mark(let node): node.json
    case .mention(let node): node.json
    case .mermaid(let node): node.json
    case .pageBreak(let node): node.json
    case .paragraph(let node): node.json
    case .poll(let node): node.json
    case .quote(let node): node.json
    case .root(let node): node.json
    case .slide(let node): node.json
    case .sticky(let node): node.json
    case .tab(let node): node.json
    case .table(let node): node.json
    case .tableCell(let node): node.json
    case .tableRow(let node): node.json
    case .text(let node): node.json
    case .thread(let node): node.json
    case .tweet(let node): node.json
    case .video(let node): node.json
    case .youTube(let node): node.json
    case .opaque(let json): json
    }
  }

  public func resolved() -> SerializedNode {
    switch self {
    case .article(let node): .article(node.resolved())
    case .autocomplete(let node): .autocomplete(node.resolved())
    case .autoLink(let node): .autoLink(node.resolved())
    case .callout(let node): .callout(node.resolved())
    case .chart(let node): .chart(node.resolved())
    case .documentCode(let node): .documentCode(node.resolved())
    case .codeHighlight(let node): .codeHighlight(node.resolved())
    case .collapsibleContainer(let node): .collapsibleContainer(node.resolved())
    case .collapsibleContent(let node): .collapsibleContent(node.resolved())
    case .collapsibleTitle(let node): .collapsibleTitle(node.resolved())
    case .comment(let node): .comment(node.resolved())
    case .emoji(let node): .emoji(node.resolved())
    case .equation(let node): .equation(node.resolved())
    case .excalidraw(let node): .excalidraw(node.resolved())
    case .figma(let node): .figma(node.resolved())
    case .footnoteDefinition(let node): .footnoteDefinition(node.resolved())
    case .footnoteReference(let node): .footnoteReference(node.resolved())
    case .hashtag(let node): .hashtag(node.resolved())
    case .heading(let node): .heading(node.resolved())
    case .horizontalRule(let node): .horizontalRule(node.resolved())
    case .image(let node): .image(node.resolved())
    case .inlineImage(let node): .inlineImage(node.resolved())
    case .keyword(let node): .keyword(node.resolved())
    case .layoutContainer(let node): .layoutContainer(node.resolved())
    case .layoutItem(let node): .layoutItem(node.resolved())
    case .lineBreak(let node): .lineBreak(node.resolved())
    case .link(let node): .link(node.resolved())
    case .list(let node): .list(node.resolved())
    case .listItem(let node): .listItem(node.resolved())
    case .mark(let node): .mark(node.resolved())
    case .mention(let node): .mention(node.resolved())
    case .mermaid(let node): .mermaid(node.resolved())
    case .pageBreak(let node): .pageBreak(node.resolved())
    case .paragraph(let node): .paragraph(node.resolved())
    case .poll(let node): .poll(node.resolved())
    case .quote(let node): .quote(node.resolved())
    case .root(let node): .root(node.resolved())
    case .slide(let node): .slide(node.resolved())
    case .sticky(let node): .sticky(node.resolved())
    case .tab(let node): .tab(node.resolved())
    case .table(let node): .table(node.resolved())
    case .tableCell(let node): .tableCell(node.resolved())
    case .tableRow(let node): .tableRow(node.resolved())
    case .text(let node): .text(node.resolved())
    case .thread(let node): .thread(node.resolved())
    case .tweet(let node): .tweet(node.resolved())
    case .video(let node): .video(node.resolved())
    case .youTube(let node): .youTube(node.resolved())
    case .opaque: self
    }
  }
}

extension NodeTraits {
  /// Every registered node's, by type.
  public static let byType: [String: NodeTraits] = [
    "article": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "autocomplete": NodeTraits(kind: .text, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "autolink": NodeTraits(kind: .element, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "callout": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(true), canBeEmpty: .fixed(false)),
    "chart": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "code": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(true)),
    "code-highlight": NodeTraits(kind: .text, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "collapsible-container": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(true)),
    "collapsible-content": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(true), canBeEmpty: .fixed(true)),
    "collapsible-title": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(true)),
    "comment": NodeTraits(kind: .decorator, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "emoji": NodeTraits(kind: .text, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "equation": NodeTraits(kind: .decorator, inline: .field("inline"), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "excalidraw": NodeTraits(kind: .decorator, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "figma": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "footnote-definition": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(true)),
    "footnote-reference": NodeTraits(kind: .decorator, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "hashtag": NodeTraits(kind: .text, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "heading": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(true)),
    "horizontalrule": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "image": NodeTraits(kind: .decorator, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "inline-image": NodeTraits(kind: .decorator, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "keyword": NodeTraits(kind: .text, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "layout-container": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(true), canBeEmpty: .fixed(false)),
    "layout-item": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(true), canBeEmpty: .fixed(true)),
    "linebreak": NodeTraits(kind: .lineBreak, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "link": NodeTraits(kind: .element, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "list": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "listitem": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(true)),
    "mark": NodeTraits(kind: .element, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "mention": NodeTraits(kind: .text, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "mermaid": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "page-break": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "paragraph": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(true)),
    "poll": NodeTraits(kind: .decorator, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "quote": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .field("shadowRoot"), canBeEmpty: .fixed(true)),
    "root": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(true)),
    "slide-deck": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "sticky": NodeTraits(kind: .decorator, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "tab": NodeTraits(kind: .text, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "table": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(true), canBeEmpty: .fixed(false)),
    "tablecell": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(true), canBeEmpty: .fixed(false)),
    "tablerow": NodeTraits(kind: .element, inline: .fixed(false), shadowRoot: .fixed(true), canBeEmpty: .fixed(false)),
    "text": NodeTraits(kind: .text, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "thread": NodeTraits(kind: .decorator, inline: .fixed(true), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "tweet": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "video": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
    "youtube": NodeTraits(kind: .decorator, inline: .fixed(false), shadowRoot: .fixed(false), canBeEmpty: .fixed(false)),
  ]
}

public enum BoxKind: String, CaseIterable, Sendable, JSONEnumeration {
  case box
}

public enum CalloutKind: String, CaseIterable, Sendable, JSONEnumeration {
  case note
  case tip
  case important
  case warning
  case caution
}

public enum ChartKind: String, CaseIterable, Sendable, JSONEnumeration {
  case chart
}

public enum ChartType: String, CaseIterable, Sendable, JSONEnumeration {
  case bar
  case line
  case area
  case pie
  case radar
  case scatter
  case composed
}

public enum CommentType: String, CaseIterable, Sendable, JSONEnumeration {
  case comment
}

public enum Direction: String, CaseIterable, Sendable, JSONEnumeration {
  case ltr
  case rtl
}

public enum ElementFormat: String, CaseIterable, Sendable, JSONEnumeration {
  case empty = ""
  case left
  case start
  case center
  case right
  case end
  case justify
}

public enum EntityMode: String, CaseIterable, Sendable, JSONEnumeration {
  case entity
}

public enum HeadingTag: String, CaseIterable, Sendable, JSONEnumeration {
  case h1
  case h2
  case h3
  case h4
  case h5
  case h6
}

public enum ImageKind: String, CaseIterable, Sendable, JSONEnumeration {
  case image
}

public enum ListTag: String, CaseIterable, Sendable, JSONEnumeration {
  case ul
  case ol
}

public enum ListType: String, CaseIterable, Sendable, JSONEnumeration {
  case number
  case bullet
  case check
}

public enum StickyNoteColor: String, CaseIterable, Sendable, JSONEnumeration {
  case pink
  case yellow
  case green
  case blue
  case red
  case orange
  case purple
  case gray
}

public enum TabMode: String, CaseIterable, Sendable, JSONEnumeration {
  case normal
}

public enum TextMode: String, CaseIterable, Sendable, JSONEnumeration {
  case normal
  case token
  case segmented
}

public enum ThreadType: String, CaseIterable, Sendable, JSONEnumeration {
  case thread
}

public enum UrlMode: String, CaseIterable, Sendable, JSONEnumeration {
  case url
}

public enum VerticalAlign: String, CaseIterable, Sendable, JSONEnumeration {
  case middle
  case bottom
}

public enum ArticleData: JSONUnion {
  case url(ArticleDataUrl)
  case entity(ArticleDataEntity)

  static let defaultValue: Self? = .url(ArticleDataUrl(["distilled": ["contentHtml": "", "title": ""], "mode": "url", "url": ""]))
  static let members: [UnionMember<Self>] = [
    .member(.object, Self.url, { if case .url(let value) = $0 { value } else { nil } }),
    .member(.object, Self.entity, { if case .entity(let value) = $0 { value } else { nil } }),
  ]
}

public enum Dimension: JSONUnion {
  case number(Double)
  case inherit

  static let defaultValue: Self? = nil
  static let members: [UnionMember<Self>] = [
    .member(.number(default: 0), Self.number, { if case .number(let value) = $0 { value } else { nil } }),
    .literal("inherit", .inherit),
  ]
}

public enum SlideElement: JSONUnion {
  case box(SlideElementBox)
  case image(SlideElementImage)
  case chart(SlideElementChart)

  static let defaultValue: Self? = nil
  static let members: [UnionMember<Self>] = [
    .member(.object, Self.box, { if case .box(let value) = $0 { value } else { nil } }),
    .member(.object, Self.image, { if case .image(let value) = $0 { value } else { nil } }),
    .member(.object, Self.chart, { if case .chart(let value) = $0 { value } else { nil } }),
  ]
}

public struct ArticleDataEntity: DeclaredObject {
  public var entityId: String?
  public var mode: EntityMode?
  public var snapshot: ArticleSnapshot?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["entityId": "", "mode": "entity"])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["entityId": Schema.entityId.fit, "mode": Schema.mode.fit, "snapshot": Schema.snapshot.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    entityId = fields.take("entityId", Schema.entityId)
    mode = fields.take("mode", Schema.mode)
    snapshot = fields.take("snapshot", Schema.snapshot)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("entityId", entityId, Schema.entityId)
    fields.put("mode", mode, Schema.mode)
    fields.put("snapshot", snapshot, Schema.snapshot)
    return fields.json
  }

  private enum Schema {
    static let entityId: FieldSchema<String> = .string(default: "")
    static let mode: FieldSchema<EntityMode> = .enumeration(default: EntityMode.entity)
    static let snapshot: FieldSchema<ArticleSnapshot> = .optional(.object)
  }
}

public struct ArticleDataUrl: DeclaredObject {
  public var distilled: ArticleDistilled?
  public var mode: UrlMode?
  public var url: String?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["distilled": ["contentHtml": "", "title": ""], "mode": "url", "url": ""])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["distilled": Schema.distilled.fit, "mode": Schema.mode.fit, "url": Schema.url.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    distilled = fields.take("distilled", Schema.distilled)
    mode = fields.take("mode", Schema.mode)
    url = fields.take("url", Schema.url)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("distilled", distilled, Schema.distilled)
    fields.put("mode", mode, Schema.mode)
    fields.put("url", url, Schema.url)
    return fields.json
  }

  private enum Schema {
    static let distilled: FieldSchema<ArticleDistilled> = .object
    static let mode: FieldSchema<UrlMode> = .enumeration(default: UrlMode.url)
    static let url: FieldSchema<String> = .string(default: "")
  }
}

public struct ArticleDistilled: DeclaredObject {
  public var bestImageUrl: Nullable<String>
  public var byline: Nullable<String>
  public var contentHtml: String?
  public var datePublished: Nullable<String>
  public var excerpt: Nullable<String>
  public var siteName: Nullable<String>
  public var title: String?
  public var updatedAt: String?
  public var wordCount: Nullable<Double>
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["contentHtml": "", "title": ""])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["bestImageUrl": Schema.bestImageUrl.fit, "byline": Schema.byline.fit, "contentHtml": Schema.contentHtml.fit, "datePublished": Schema.datePublished.fit, "excerpt": Schema.excerpt.fit, "siteName": Schema.siteName.fit, "title": Schema.title.fit, "updatedAt": Schema.updatedAt.fit, "wordCount": Schema.wordCount.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    bestImageUrl = fields.takeNullable("bestImageUrl", Schema.bestImageUrl)
    byline = fields.takeNullable("byline", Schema.byline)
    contentHtml = fields.take("contentHtml", Schema.contentHtml)
    datePublished = fields.takeNullable("datePublished", Schema.datePublished)
    excerpt = fields.takeNullable("excerpt", Schema.excerpt)
    siteName = fields.takeNullable("siteName", Schema.siteName)
    title = fields.take("title", Schema.title)
    updatedAt = fields.take("updatedAt", Schema.updatedAt)
    wordCount = fields.takeNullable("wordCount", Schema.wordCount)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.putNullable("bestImageUrl", bestImageUrl, Schema.bestImageUrl)
    fields.putNullable("byline", byline, Schema.byline)
    fields.put("contentHtml", contentHtml, Schema.contentHtml)
    fields.putNullable("datePublished", datePublished, Schema.datePublished)
    fields.putNullable("excerpt", excerpt, Schema.excerpt)
    fields.putNullable("siteName", siteName, Schema.siteName)
    fields.put("title", title, Schema.title)
    fields.put("updatedAt", updatedAt, Schema.updatedAt)
    fields.putNullable("wordCount", wordCount, Schema.wordCount)
    return fields.json
  }

  private enum Schema {
    static let bestImageUrl: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let byline: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let contentHtml: FieldSchema<String> = .string(default: "")
    static let datePublished: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let excerpt: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let siteName: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let title: FieldSchema<String> = .string(default: "")
    static let updatedAt: FieldSchema<String> = .optional(.string(default: ""))
    static let wordCount: FieldSchema<Double?> = .optional(.nullable(.number(default: 0)))
  }
}

public struct ArticleSnapshot: DeclaredObject {
  public var bestImageUrl: Nullable<String>
  public var byline: Nullable<String>
  public var contentHtml: String?
  public var siteName: Nullable<String>
  public var title: String?
  public var updatedAt: String?
  public var wordCount: Nullable<Double>
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["contentHtml": "", "title": ""])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["bestImageUrl": Schema.bestImageUrl.fit, "byline": Schema.byline.fit, "contentHtml": Schema.contentHtml.fit, "siteName": Schema.siteName.fit, "title": Schema.title.fit, "updatedAt": Schema.updatedAt.fit, "wordCount": Schema.wordCount.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    bestImageUrl = fields.takeNullable("bestImageUrl", Schema.bestImageUrl)
    byline = fields.takeNullable("byline", Schema.byline)
    contentHtml = fields.take("contentHtml", Schema.contentHtml)
    siteName = fields.takeNullable("siteName", Schema.siteName)
    title = fields.take("title", Schema.title)
    updatedAt = fields.take("updatedAt", Schema.updatedAt)
    wordCount = fields.takeNullable("wordCount", Schema.wordCount)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.putNullable("bestImageUrl", bestImageUrl, Schema.bestImageUrl)
    fields.putNullable("byline", byline, Schema.byline)
    fields.put("contentHtml", contentHtml, Schema.contentHtml)
    fields.putNullable("siteName", siteName, Schema.siteName)
    fields.put("title", title, Schema.title)
    fields.put("updatedAt", updatedAt, Schema.updatedAt)
    fields.putNullable("wordCount", wordCount, Schema.wordCount)
    return fields.json
  }

  private enum Schema {
    static let bestImageUrl: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let byline: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let contentHtml: FieldSchema<String> = .string(default: "")
    static let siteName: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let title: FieldSchema<String> = .string(default: "")
    static let updatedAt: FieldSchema<String> = .optional(.string(default: ""))
    static let wordCount: FieldSchema<Double?> = .optional(.nullable(.number(default: 0)))
  }
}

public struct ColorPalette: DeclaredObject {
  public var accent: String?
  public var primary: String?
  public var secondary: String?
  public var slideBackground: String?
  public var textBody: String?
  public var textHeader: String?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self([:])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["accent": Schema.accent.fit, "primary": Schema.primary.fit, "secondary": Schema.secondary.fit, "slideBackground": Schema.slideBackground.fit, "textBody": Schema.textBody.fit, "textHeader": Schema.textHeader.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    accent = fields.take("accent", Schema.accent)
    primary = fields.take("primary", Schema.primary)
    secondary = fields.take("secondary", Schema.secondary)
    slideBackground = fields.take("slideBackground", Schema.slideBackground)
    textBody = fields.take("textBody", Schema.textBody)
    textHeader = fields.take("textHeader", Schema.textHeader)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("accent", accent, Schema.accent)
    fields.put("primary", primary, Schema.primary)
    fields.put("secondary", secondary, Schema.secondary)
    fields.put("slideBackground", slideBackground, Schema.slideBackground)
    fields.put("textBody", textBody, Schema.textBody)
    fields.put("textHeader", textHeader, Schema.textHeader)
    return fields.json
  }

  private enum Schema {
    static let accent: FieldSchema<String> = .optional(.string(default: ""))
    static let primary: FieldSchema<String> = .optional(.string(default: ""))
    static let secondary: FieldSchema<String> = .optional(.string(default: ""))
    static let slideBackground: FieldSchema<String> = .optional(.string(default: ""))
    static let textBody: FieldSchema<String> = .optional(.string(default: ""))
    static let textHeader: FieldSchema<String> = .optional(.string(default: ""))
  }
}

public struct Comment: DeclaredObject {
  public var author: String?
  public var content: String?
  public var deleted: Bool?
  public var id: String?
  public var timeStamp: Double?
  public var type: CommentType?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["author": "", "content": "", "deleted": false, "id": "", "timeStamp": 0, "type": "comment"])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["author": Schema.author.fit, "content": Schema.content.fit, "deleted": Schema.deleted.fit, "id": Schema.id.fit, "timeStamp": Schema.timeStamp.fit, "type": Schema.type.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    author = fields.take("author", Schema.author)
    content = fields.take("content", Schema.content)
    deleted = fields.take("deleted", Schema.deleted)
    id = fields.take("id", Schema.id)
    timeStamp = fields.take("timeStamp", Schema.timeStamp)
    type = fields.take("type", Schema.type)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("author", author, Schema.author)
    fields.put("content", content, Schema.content)
    fields.put("deleted", deleted, Schema.deleted)
    fields.put("id", id, Schema.id)
    fields.put("timeStamp", timeStamp, Schema.timeStamp)
    fields.put("type", type, Schema.type)
    return fields.json
  }

  private enum Schema {
    static let author: FieldSchema<String> = .string(default: "")
    static let content: FieldSchema<String> = .string(default: "")
    static let deleted: FieldSchema<Bool> = .boolean(default: false)
    static let id: FieldSchema<String> = .string(default: "")
    static let timeStamp: FieldSchema<Double> = .number(default: 0)
    static let type: FieldSchema<CommentType> = .enumeration(default: CommentType.comment)
  }
}

public struct CommentThread: DeclaredObject {
  public var comments: [Comment]?
  public var id: String?
  public var quote: String?
  public var resolved: Bool?
  public var type: ThreadType?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["comments": [], "id": "", "quote": "", "type": "thread"])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["comments": Schema.comments.fit, "id": Schema.id.fit, "quote": Schema.quote.fit, "resolved": Schema.resolved.fit, "type": Schema.type.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    comments = fields.take("comments", Schema.comments)
    id = fields.take("id", Schema.id)
    quote = fields.take("quote", Schema.quote)
    resolved = fields.take("resolved", Schema.resolved)
    type = fields.take("type", Schema.type)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("comments", comments, Schema.comments)
    fields.put("id", id, Schema.id)
    fields.put("quote", quote, Schema.quote)
    fields.put("resolved", resolved, Schema.resolved)
    fields.put("type", type, Schema.type)
    return fields.json
  }

  private enum Schema {
    static let comments: FieldSchema<[Comment]> = .array(.object)
    static let id: FieldSchema<String> = .string(default: "")
    static let quote: FieldSchema<String> = .string(default: "")
    static let resolved: FieldSchema<Bool> = .optional(.boolean(default: false))
    static let type: FieldSchema<ThreadType> = .enumeration(default: ThreadType.thread)
  }
}

public struct DeckFonts: DeclaredObject {
  public var body: String?
  public var caption: String?
  public var heading: String?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self([:])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["body": Schema.body.fit, "caption": Schema.caption.fit, "heading": Schema.heading.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    body = fields.take("body", Schema.body)
    caption = fields.take("caption", Schema.caption)
    heading = fields.take("heading", Schema.heading)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("body", body, Schema.body)
    fields.put("caption", caption, Schema.caption)
    fields.put("heading", heading, Schema.heading)
    return fields.json
  }

  private enum Schema {
    static let body: FieldSchema<String> = .optional(.string(default: ""))
    static let caption: FieldSchema<String> = .optional(.string(default: ""))
    static let heading: FieldSchema<String> = .optional(.string(default: ""))
  }
}

public struct DeckMetadata: DeclaredObject {
  public var audiencePersonaSummary: String?
  public var bigIdea: String?
  public var originalUserPrompt: String?
  public var overallObjective: String?
  public var recommendedTone: String?
  public var targetDurationMinutes: Double?
  public var targetSlideCount: Double?
  public var theme: DeckTheme?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self([:])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["audiencePersonaSummary": Schema.audiencePersonaSummary.fit, "bigIdea": Schema.bigIdea.fit, "originalUserPrompt": Schema.originalUserPrompt.fit, "overallObjective": Schema.overallObjective.fit, "recommendedTone": Schema.recommendedTone.fit, "targetDurationMinutes": Schema.targetDurationMinutes.fit, "targetSlideCount": Schema.targetSlideCount.fit, "theme": Schema.theme.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    audiencePersonaSummary = fields.take("audiencePersonaSummary", Schema.audiencePersonaSummary)
    bigIdea = fields.take("bigIdea", Schema.bigIdea)
    originalUserPrompt = fields.take("originalUserPrompt", Schema.originalUserPrompt)
    overallObjective = fields.take("overallObjective", Schema.overallObjective)
    recommendedTone = fields.take("recommendedTone", Schema.recommendedTone)
    targetDurationMinutes = fields.take("targetDurationMinutes", Schema.targetDurationMinutes)
    targetSlideCount = fields.take("targetSlideCount", Schema.targetSlideCount)
    theme = fields.take("theme", Schema.theme)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("audiencePersonaSummary", audiencePersonaSummary, Schema.audiencePersonaSummary)
    fields.put("bigIdea", bigIdea, Schema.bigIdea)
    fields.put("originalUserPrompt", originalUserPrompt, Schema.originalUserPrompt)
    fields.put("overallObjective", overallObjective, Schema.overallObjective)
    fields.put("recommendedTone", recommendedTone, Schema.recommendedTone)
    fields.put("targetDurationMinutes", targetDurationMinutes, Schema.targetDurationMinutes)
    fields.put("targetSlideCount", targetSlideCount, Schema.targetSlideCount)
    fields.put("theme", theme, Schema.theme)
    return fields.json
  }

  private enum Schema {
    static let audiencePersonaSummary: FieldSchema<String> = .optional(.string(default: ""))
    static let bigIdea: FieldSchema<String> = .optional(.string(default: ""))
    static let originalUserPrompt: FieldSchema<String> = .optional(.string(default: ""))
    static let overallObjective: FieldSchema<String> = .optional(.string(default: ""))
    static let recommendedTone: FieldSchema<String> = .optional(.string(default: ""))
    static let targetDurationMinutes: FieldSchema<Double> = .optional(.number(default: 0))
    static let targetSlideCount: FieldSchema<Double> = .optional(.number(default: 0))
    static let theme: FieldSchema<DeckTheme> = .optional(.object)
  }
}

public struct DeckTheme: DeclaredObject {
  public var colorPalette: ColorPalette?
  public var customTokens: String?
  public var fonts: DeckFonts?
  public var logoUrl: String?
  public var templateName: String?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self([:])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["colorPalette": Schema.colorPalette.fit, "customTokens": Schema.customTokens.fit, "fonts": Schema.fonts.fit, "logoUrl": Schema.logoUrl.fit, "templateName": Schema.templateName.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    colorPalette = fields.take("colorPalette", Schema.colorPalette)
    customTokens = fields.take("customTokens", Schema.customTokens)
    fonts = fields.take("fonts", Schema.fonts)
    logoUrl = fields.take("logoUrl", Schema.logoUrl)
    templateName = fields.take("templateName", Schema.templateName)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("colorPalette", colorPalette, Schema.colorPalette)
    fields.put("customTokens", customTokens, Schema.customTokens)
    fields.put("fonts", fonts, Schema.fonts)
    fields.put("logoUrl", logoUrl, Schema.logoUrl)
    fields.put("templateName", templateName, Schema.templateName)
    return fields.json
  }

  private enum Schema {
    static let colorPalette: FieldSchema<ColorPalette> = .optional(.object)
    static let customTokens: FieldSchema<String> = .optional(.string(default: ""))
    static let fonts: FieldSchema<DeckFonts> = .optional(.object)
    static let logoUrl: FieldSchema<String> = .optional(.string(default: ""))
    static let templateName: FieldSchema<String> = .optional(.string(default: ""))
  }
}

public struct Figure: DeclaredObject {
  public var caption: String?
  public var width: String?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self([:])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["caption": Schema.caption.fit, "width": Schema.width.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    caption = fields.take("caption", Schema.caption)
    width = fields.take("width", Schema.width)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("caption", caption, Schema.caption)
    fields.put("width", width, Schema.width)
    return fields.json
  }

  private enum Schema {
    static let caption: FieldSchema<String> = .optional(.string(default: ""))
    static let width: FieldSchema<String> = .optional(.transform(.string(default: ""), Transforms.figureWidth))
  }
}

public struct NaturalSize: DeclaredObject {
  public var height: Double?
  public var width: Double?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["height": 0, "width": 0])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["height": Schema.height.fit, "width": Schema.width.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    height = fields.take("height", Schema.height)
    width = fields.take("width", Schema.width)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("height", height, Schema.height)
    fields.put("width", width, Schema.width)
    return fields.json
  }

  private enum Schema {
    static let height: FieldSchema<Double> = .number(default: 0)
    static let width: FieldSchema<Double> = .number(default: 0)
  }
}

public struct NestedEditor: DeclaredObject {
  public var editorState: JSONValue?
  public var unknownFields: [String: JSONValue]

  static let isOpen = false
  static let defaultValue = Self(["editorState": ["root": ["children": [], "direction": nil, "format": "", "indent": 0, "type": "root", "version": 1]]])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["editorState": Schema.editorState.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    editorState = fields.take("editorState", Schema.editorState)
    unknownFields = [:]
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("editorState", editorState, Schema.editorState)
    return fields.json
  }

  private enum Schema {
    static let editorState: FieldSchema<JSONValue> = .transform(.rawOr(["root": ["children": [], "direction": nil, "format": "", "indent": 0, "type": "root", "version": 1]]), Transforms.nestedEditorState)
  }
}

public struct PollOption: DeclaredObject {
  public var text: String?
  public var uid: String?
  public var votes: [String]?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["text": "", "uid": "", "votes": []])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["text": Schema.text.fit, "uid": Schema.uid.fit, "votes": Schema.votes.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    text = fields.take("text", Schema.text)
    uid = fields.take("uid", Schema.uid)
    votes = fields.take("votes", Schema.votes)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("text", text, Schema.text)
    fields.put("uid", uid, Schema.uid)
    fields.put("votes", votes, Schema.votes)
    return fields.json
  }

  private enum Schema {
    static let text: FieldSchema<String> = .string(default: "")
    static let uid: FieldSchema<String> = .string(default: "")
    static let votes: FieldSchema<[String]> = .array(.string(default: ""))
  }
}

public struct Slide: DeclaredObject {
  public var backgroundColor: String?
  public var elements: [SlideElement]?
  public var id: String?
  public var slideMetadata: SlideMetadata?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["elements": [], "id": ""])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["backgroundColor": Schema.backgroundColor.fit, "elements": Schema.elements.fit, "id": Schema.id.fit, "slideMetadata": Schema.slideMetadata.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    backgroundColor = fields.take("backgroundColor", Schema.backgroundColor)
    elements = fields.take("elements", Schema.elements)
    id = fields.take("id", Schema.id)
    slideMetadata = fields.take("slideMetadata", Schema.slideMetadata)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("backgroundColor", backgroundColor, Schema.backgroundColor)
    fields.put("elements", elements, Schema.elements)
    fields.put("id", id, Schema.id)
    fields.put("slideMetadata", slideMetadata, Schema.slideMetadata)
    return fields.json
  }

  private enum Schema {
    static let backgroundColor: FieldSchema<String> = .optional(.string(default: ""))
    static let elements: FieldSchema<[SlideElement]> = .array(.union)
    static let id: FieldSchema<String> = .string(default: "")
    static let slideMetadata: FieldSchema<SlideMetadata> = .optional(.object)
  }
}

public struct SlideDeck: DeclaredObject {
  public var currentSlideId: Nullable<String>
  public var deckMetadata: DeckMetadata?
  public var slides: [Slide]?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["currentSlideId": nil, "slides": []])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["currentSlideId": Schema.currentSlideId.fit, "deckMetadata": Schema.deckMetadata.fit, "slides": Schema.slides.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    currentSlideId = fields.takeNullable("currentSlideId", Schema.currentSlideId)
    deckMetadata = fields.take("deckMetadata", Schema.deckMetadata)
    slides = fields.take("slides", Schema.slides)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.putNullable("currentSlideId", currentSlideId, Schema.currentSlideId)
    fields.put("deckMetadata", deckMetadata, Schema.deckMetadata)
    fields.put("slides", slides, Schema.slides)
    return fields.json
  }

  private enum Schema {
    static let currentSlideId: FieldSchema<String?> = .nullable(.string(default: ""))
    static let deckMetadata: FieldSchema<DeckMetadata> = .optional(.object)
    static let slides: FieldSchema<[Slide]> = .array(.object)
  }
}

public struct SlideElementBox: DeclaredObject {
  public var backgroundColor: String?
  public var editorStateJSON: JSONValue?
  public var height: Dimension?
  public var id: String?
  public var kind: BoxKind?
  public var version: Double?
  public var width: Dimension?
  public var x: Double?
  public var y: Double?
  public var zIndex: Double?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["id": "", "kind": "box", "x": 0, "y": 0, "zIndex": 0])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["backgroundColor": Schema.backgroundColor.fit, "editorStateJSON": Schema.editorStateJSON.fit, "height": Schema.height.fit, "id": Schema.id.fit, "kind": Schema.kind.fit, "version": Schema.version.fit, "width": Schema.width.fit, "x": Schema.x.fit, "y": Schema.y.fit, "zIndex": Schema.zIndex.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    backgroundColor = fields.take("backgroundColor", Schema.backgroundColor)
    editorStateJSON = fields.take("editorStateJSON", Schema.editorStateJSON)
    height = fields.take("height", Schema.height)
    id = fields.take("id", Schema.id)
    kind = fields.take("kind", Schema.kind)
    version = fields.take("version", Schema.version)
    width = fields.take("width", Schema.width)
    x = fields.take("x", Schema.x)
    y = fields.take("y", Schema.y)
    zIndex = fields.take("zIndex", Schema.zIndex)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("backgroundColor", backgroundColor, Schema.backgroundColor)
    fields.put("editorStateJSON", editorStateJSON, Schema.editorStateJSON)
    fields.put("height", height, Schema.height)
    fields.put("id", id, Schema.id)
    fields.put("kind", kind, Schema.kind)
    fields.put("version", version, Schema.version)
    fields.put("width", width, Schema.width)
    fields.put("x", x, Schema.x)
    fields.put("y", y, Schema.y)
    fields.put("zIndex", zIndex, Schema.zIndex)
    return fields.json
  }

  private enum Schema {
    static let backgroundColor: FieldSchema<String> = .optional(.string(default: ""))
    static let editorStateJSON: FieldSchema<JSONValue> = .raw
    static let height: FieldSchema<Dimension> = .union
    static let id: FieldSchema<String> = .string(default: "")
    static let kind: FieldSchema<BoxKind> = .enumeration(default: BoxKind.box)
    static let version: FieldSchema<Double> = .optional(.number(default: 0))
    static let width: FieldSchema<Dimension> = .union
    static let x: FieldSchema<Double> = .number(default: 0)
    static let y: FieldSchema<Double> = .number(default: 0)
    static let zIndex: FieldSchema<Double> = .number(default: 0)
  }
}

public struct SlideElementChart: DeclaredObject {
  public var chartConfig: String?
  public var chartData: String?
  public var chartType: ChartType?
  public var height: Dimension?
  public var id: String?
  public var kind: ChartKind?
  public var version: Double?
  public var width: Dimension?
  public var x: Double?
  public var y: Double?
  public var zIndex: Double?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["chartConfig": "", "chartData": "", "chartType": "bar", "id": "", "kind": "chart", "x": 0, "y": 0, "zIndex": 0])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["chartConfig": Schema.chartConfig.fit, "chartData": Schema.chartData.fit, "chartType": Schema.chartType.fit, "height": Schema.height.fit, "id": Schema.id.fit, "kind": Schema.kind.fit, "version": Schema.version.fit, "width": Schema.width.fit, "x": Schema.x.fit, "y": Schema.y.fit, "zIndex": Schema.zIndex.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    chartConfig = fields.take("chartConfig", Schema.chartConfig)
    chartData = fields.take("chartData", Schema.chartData)
    chartType = fields.take("chartType", Schema.chartType)
    height = fields.take("height", Schema.height)
    id = fields.take("id", Schema.id)
    kind = fields.take("kind", Schema.kind)
    version = fields.take("version", Schema.version)
    width = fields.take("width", Schema.width)
    x = fields.take("x", Schema.x)
    y = fields.take("y", Schema.y)
    zIndex = fields.take("zIndex", Schema.zIndex)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("chartConfig", chartConfig, Schema.chartConfig)
    fields.put("chartData", chartData, Schema.chartData)
    fields.put("chartType", chartType, Schema.chartType)
    fields.put("height", height, Schema.height)
    fields.put("id", id, Schema.id)
    fields.put("kind", kind, Schema.kind)
    fields.put("version", version, Schema.version)
    fields.put("width", width, Schema.width)
    fields.put("x", x, Schema.x)
    fields.put("y", y, Schema.y)
    fields.put("zIndex", zIndex, Schema.zIndex)
    return fields.json
  }

  private enum Schema {
    static let chartConfig: FieldSchema<String> = .string(default: "")
    static let chartData: FieldSchema<String> = .string(default: "")
    static let chartType: FieldSchema<ChartType> = .enumeration(default: ChartType.bar)
    static let height: FieldSchema<Dimension> = .union
    static let id: FieldSchema<String> = .string(default: "")
    static let kind: FieldSchema<ChartKind> = .enumeration(default: ChartKind.chart)
    static let version: FieldSchema<Double> = .optional(.number(default: 0))
    static let width: FieldSchema<Dimension> = .union
    static let x: FieldSchema<Double> = .number(default: 0)
    static let y: FieldSchema<Double> = .number(default: 0)
    static let zIndex: FieldSchema<Double> = .number(default: 0)
  }
}

public struct SlideElementImage: DeclaredObject {
  public var height: Dimension?
  public var id: String?
  public var kind: ImageKind?
  public var url: String?
  public var version: Double?
  public var width: Dimension?
  public var x: Double?
  public var y: Double?
  public var zIndex: Double?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self(["id": "", "kind": "image", "url": "", "x": 0, "y": 0, "zIndex": 0])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["height": Schema.height.fit, "id": Schema.id.fit, "kind": Schema.kind.fit, "url": Schema.url.fit, "version": Schema.version.fit, "width": Schema.width.fit, "x": Schema.x.fit, "y": Schema.y.fit, "zIndex": Schema.zIndex.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    height = fields.take("height", Schema.height)
    id = fields.take("id", Schema.id)
    kind = fields.take("kind", Schema.kind)
    url = fields.take("url", Schema.url)
    version = fields.take("version", Schema.version)
    width = fields.take("width", Schema.width)
    x = fields.take("x", Schema.x)
    y = fields.take("y", Schema.y)
    zIndex = fields.take("zIndex", Schema.zIndex)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("height", height, Schema.height)
    fields.put("id", id, Schema.id)
    fields.put("kind", kind, Schema.kind)
    fields.put("url", url, Schema.url)
    fields.put("version", version, Schema.version)
    fields.put("width", width, Schema.width)
    fields.put("x", x, Schema.x)
    fields.put("y", y, Schema.y)
    fields.put("zIndex", zIndex, Schema.zIndex)
    return fields.json
  }

  private enum Schema {
    static let height: FieldSchema<Dimension> = .union
    static let id: FieldSchema<String> = .string(default: "")
    static let kind: FieldSchema<ImageKind> = .enumeration(default: ImageKind.image)
    static let url: FieldSchema<String> = .string(default: "")
    static let version: FieldSchema<Double> = .optional(.number(default: 0))
    static let width: FieldSchema<Dimension> = .union
    static let x: FieldSchema<Double> = .number(default: 0)
    static let y: FieldSchema<Double> = .number(default: 0)
    static let zIndex: FieldSchema<Double> = .number(default: 0)
  }
}

public struct SlideMetadata: DeclaredObject {
  public var keyMessage: String?
  public var keyVisualHint: String?
  public var layoutTemplateHint: String?
  public var purpose: String?
  public var sourceMaterialRefs: [String]?
  public var speakerNotes: String?
  public var storyboardTitle: String?
  public var takeAwayMessage: String?
  public var unknownFields: [String: JSONValue]

  static let isOpen = true
  static let defaultValue = Self([:])
  static let fieldFits: [String: @Sendable (JSONValue) -> Fit] = ["keyMessage": Schema.keyMessage.fit, "keyVisualHint": Schema.keyVisualHint.fit, "layoutTemplateHint": Schema.layoutTemplateHint.fit, "purpose": Schema.purpose.fit, "sourceMaterialRefs": Schema.sourceMaterialRefs.fit, "speakerNotes": Schema.speakerNotes.fit, "storyboardTitle": Schema.storyboardTitle.fit, "takeAwayMessage": Schema.takeAwayMessage.fit]

  init(_ object: [String: JSONValue]) {
    var fields = NodeFields(object)
    keyMessage = fields.take("keyMessage", Schema.keyMessage)
    keyVisualHint = fields.take("keyVisualHint", Schema.keyVisualHint)
    layoutTemplateHint = fields.take("layoutTemplateHint", Schema.layoutTemplateHint)
    purpose = fields.take("purpose", Schema.purpose)
    sourceMaterialRefs = fields.take("sourceMaterialRefs", Schema.sourceMaterialRefs)
    speakerNotes = fields.take("speakerNotes", Schema.speakerNotes)
    storyboardTitle = fields.take("storyboardTitle", Schema.storyboardTitle)
    takeAwayMessage = fields.take("takeAwayMessage", Schema.takeAwayMessage)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(over: unknownFields)
    fields.put("keyMessage", keyMessage, Schema.keyMessage)
    fields.put("keyVisualHint", keyVisualHint, Schema.keyVisualHint)
    fields.put("layoutTemplateHint", layoutTemplateHint, Schema.layoutTemplateHint)
    fields.put("purpose", purpose, Schema.purpose)
    fields.put("sourceMaterialRefs", sourceMaterialRefs, Schema.sourceMaterialRefs)
    fields.put("speakerNotes", speakerNotes, Schema.speakerNotes)
    fields.put("storyboardTitle", storyboardTitle, Schema.storyboardTitle)
    fields.put("takeAwayMessage", takeAwayMessage, Schema.takeAwayMessage)
    return fields.json
  }

  private enum Schema {
    static let keyMessage: FieldSchema<String> = .optional(.string(default: ""))
    static let keyVisualHint: FieldSchema<String> = .optional(.string(default: ""))
    static let layoutTemplateHint: FieldSchema<String> = .optional(.string(default: ""))
    static let purpose: FieldSchema<String> = .optional(.string(default: ""))
    static let sourceMaterialRefs: FieldSchema<[String]> = .optional(.array(.string(default: "")))
    static let speakerNotes: FieldSchema<String> = .optional(.string(default: ""))
    static let storyboardTitle: FieldSchema<String> = .optional(.string(default: ""))
    static let takeAwayMessage: FieldSchema<String> = .optional(.string(default: ""))
  }
}

public struct SerializedArticleNode: NodePayload {
  public static let type = "article"
  public static let version = 1
  public var data: Shaped<ArticleData>?
  public var format: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    data = fields.take("data", Schema.data)
    format = fields.take("format", Schema.format)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("data", data, Schema.data)
    fields.put("format", format, Schema.format)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.data = Schema.data.resolving(data)
    node.format = Schema.format.resolving(format)
    return node
  }

  private enum Schema {
    static let data: FieldSchema<Shaped<ArticleData>> = .shaped(.union, .raw)
    static let format: FieldSchema<JSONValue> = .transform(.raw, Transforms.emptyOrStored)
  }
}

public struct SerializedAutocompleteNode: NodePayload {
  public static let type = "autocomplete"
  public static let version = 1
  public var detail: JSONValue?
  public var format: JSONValue?
  public var mode: TextMode?
  public var style: JSONValue?
  public var text: JSONValue?
  public var uuid: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    detail = fields.take("detail", Schema.detail)
    format = fields.take("format", Schema.format)
    mode = fields.take("mode", Schema.mode)
    style = fields.take("style", Schema.style)
    text = fields.take("text", Schema.text)
    uuid = fields.take("uuid", Schema.uuid)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("detail", detail, Schema.detail)
    fields.put("format", format, Schema.format)
    fields.put("mode", mode, Schema.mode)
    fields.put("style", style, Schema.style)
    fields.put("text", text, Schema.text)
    fields.put("uuid", uuid, Schema.uuid)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.detail = Schema.detail.resolving(detail)
    node.format = Schema.format.resolving(format)
    node.mode = Schema.mode.resolving(mode)
    node.style = Schema.style.resolving(style)
    node.text = Schema.text.resolving(text)
    node.uuid = Schema.uuid.resolving(uuid)
    return node
  }

  private enum Schema {
    static let detail: FieldSchema<JSONValue> = .aliased(.transform(.raw, Transforms.stringAbsent), ["directionless": 1, "unmergeable": 2])
    static let format: FieldSchema<JSONValue> = .aliased(.transform(.raw, Transforms.stringAbsent), ["bold": 1, "capitalize": 1024, "code": 16, "highlight": 128, "italic": 2, "lowercase": 256, "strikethrough": 4, "subscript": 32, "superscript": 64, "underline": 8, "uppercase": 512])
    static let mode: FieldSchema<TextMode> = .enumeration(default: nil)
    static let style: FieldSchema<JSONValue> = .raw
    static let text: FieldSchema<JSONValue> = .rawOr("")
    static let uuid: FieldSchema<JSONValue> = .raw
  }
}

public struct SerializedAutoLinkNode: ParentNodePayload {
  public static let type = "autolink"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var isUnlinked: Bool?
  public var rel: Nullable<String>
  public var target: Nullable<String>
  public var textFormat: Double?
  public var textStyle: String?
  public var title: Nullable<String>
  public var url: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    isUnlinked = fields.take("isUnlinked", Schema.isUnlinked)
    rel = fields.takeNullable("rel", Schema.rel)
    target = fields.takeNullable("target", Schema.target)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    title = fields.takeNullable("title", Schema.title)
    url = fields.take("url", Schema.url)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("isUnlinked", isUnlinked, Schema.isUnlinked)
    fields.putNullable("rel", rel, Schema.rel)
    fields.putNullable("target", target, Schema.target)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    fields.putNullable("title", title, Schema.title)
    fields.put("url", url, Schema.url)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.isUnlinked = Schema.isUnlinked.resolving(isUnlinked)
    node.rel = Schema.rel.resolving(rel)
    node.target = Schema.target.resolving(target)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    node.title = Schema.title.resolving(title)
    node.url = Schema.url.resolving(url)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let isUnlinked: FieldSchema<Bool> = .boolean(default: false)
    static let rel: FieldSchema<String?> = .nullable(.string(default: ""), defaultAsNull: true)
    static let target: FieldSchema<String?> = .nullable(.string(default: ""), defaultAsNull: true)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
    static let title: FieldSchema<String?> = .nullable(.string(default: ""), defaultAsNull: true)
    static let url: FieldSchema<String> = .string(default: "")
  }
}

public struct SerializedCalloutNode: ParentNodePayload {
  public static let type = "callout"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var kind: CalloutKind?
  public var textFormat: Double?
  public var textStyle: String?
  public var title: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    kind = fields.take("kind", Schema.kind)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    title = fields.take("title", Schema.title)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("kind", kind, Schema.kind)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    fields.put("title", title, Schema.title)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.kind = Schema.kind.resolving(kind)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    node.title = Schema.title.resolving(title)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let kind: FieldSchema<CalloutKind> = .enumeration(default: CalloutKind.note)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
    static let title: FieldSchema<String> = .string(default: "")
  }
}

public struct SerializedChartNode: NodePayload {
  public static let type = "chart"
  public static let version = 1
  public var chartConfig: JSONValue?
  public var chartData: JSONValue?
  public var chartType: JSONValue?
  public var figure: Shaped<Figure>?
  public var height: JSONValue?
  public var width: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    var state = fields.takeState()
    chartConfig = fields.take("chartConfig", Schema.chartConfig)
    chartData = fields.take("chartData", Schema.chartData)
    chartType = fields.take("chartType", Schema.chartType)
    figure = state.take("figure", Schema.figure)
    height = fields.take("height", Schema.height)
    width = fields.take("width", Schema.width)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    var state = fields.takeState()
    fields.put("chartConfig", chartConfig, Schema.chartConfig)
    fields.put("chartData", chartData, Schema.chartData)
    fields.put("chartType", chartType, Schema.chartType)
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("height", height, Schema.height)
    fields.put("width", width, Schema.width)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.chartConfig = Schema.chartConfig.resolving(chartConfig)
    node.chartData = Schema.chartData.resolving(chartData)
    node.chartType = Schema.chartType.resolving(chartType)
    node.figure = Schema.figure.omittingDefault(figure)
    node.height = Schema.height.resolving(height)
    node.width = Schema.width.resolving(width)
    return node
  }

  private enum Schema {
    static let chartConfig: FieldSchema<JSONValue> = .rawOr("{}")
    static let chartData: FieldSchema<JSONValue> = .rawOr("[]")
    static let chartType: FieldSchema<JSONValue> = .rawOr("bar")
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let height: FieldSchema<JSONValue> = .transform(.rawOr("inherit"), Transforms.zeroAsInherit)
    static let width: FieldSchema<JSONValue> = .transform(.rawOr("inherit"), Transforms.zeroAsInherit)
  }
}

public struct SerializedDocumentCodeNode: ParentNodePayload {
  public static let type = "code"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var language: String?
  public var showLineNumbers: JSONValue?
  public var textFormat: Double?
  public var textStyle: String?
  public var theme: Never?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    language = fields.take("language", Schema.language)
    showLineNumbers = fields.take("showLineNumbers", Schema.showLineNumbers)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    theme = fields.take("theme", Schema.theme)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("language", language, Schema.language)
    fields.put("showLineNumbers", showLineNumbers, Schema.showLineNumbers)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    fields.put("theme", theme, Schema.theme)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.language = Schema.language.resolving(language)
    node.showLineNumbers = Schema.showLineNumbers.resolving(showLineNumbers)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    node.theme = Schema.theme.resolving(theme)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let language: FieldSchema<String> = .optional(.transform(.string(default: ""), Transforms.emptyAbsent))
    static let showLineNumbers: FieldSchema<JSONValue> = .rawOr(false, nullAsAbsent: true)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
    static let theme: FieldSchema<Never> = .enumeration(default: nil)
  }
}

public struct SerializedCodeHighlightNode: NodePayload {
  public static let type = "code-highlight"
  public static let version = 1
  public var detail: Double?
  public var format: Double?
  public var highlightType: Nullable<String>
  public var mode: TextMode?
  public var style: String?
  public var text: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    detail = fields.take("detail", Schema.detail)
    format = fields.take("format", Schema.format)
    highlightType = fields.takeNullable("highlightType", Schema.highlightType)
    mode = fields.take("mode", Schema.mode)
    style = fields.take("style", Schema.style)
    text = fields.take("text", Schema.text)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("detail", detail, Schema.detail)
    fields.put("format", format, Schema.format)
    fields.putNullable("highlightType", highlightType, Schema.highlightType)
    fields.put("mode", mode, Schema.mode)
    fields.put("style", style, Schema.style)
    fields.put("text", text, Schema.text)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.detail = Schema.detail.resolving(detail)
    node.format = Schema.format.resolving(format)
    node.highlightType = Schema.highlightType.resolving(highlightType)
    node.mode = Schema.mode.resolving(mode)
    node.style = Schema.style.resolving(style)
    node.text = Schema.text.resolving(text)
    return node
  }

  private enum Schema {
    static let detail: FieldSchema<Double> = .aliased(.number(default: 0), ["directionless": 1, "unmergeable": 2])
    static let format: FieldSchema<Double> = .aliased(.number(default: 0), ["bold": 1, "capitalize": 1024, "code": 16, "highlight": 128, "italic": 2, "lowercase": 256, "strikethrough": 4, "subscript": 32, "superscript": 64, "underline": 8, "uppercase": 512])
    static let highlightType: FieldSchema<String?> = .optional(.nullable(.string(default: "")))
    static let mode: FieldSchema<TextMode> = .enumeration(default: TextMode.normal)
    static let style: FieldSchema<String> = .string(default: "")
    static let text: FieldSchema<String> = .string(default: "")
  }
}

public struct SerializedCollapsibleContainerNode: ParentNodePayload {
  public static let type = "collapsible-container"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Double?
  public var `open`: JSONValue?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    `open` = fields.take("open", Schema.`open`)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("open", `open`, Schema.`open`)
    fields.putUnlessDefault("textFormat", textFormat, Schema.textFormat)
    fields.putUnlessDefault("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.`open` = Schema.`open`.resolving(`open`)
    node.textFormat = Schema.textFormat.omittingDefault(textFormat)
    node.textStyle = Schema.textStyle.omittingDefault(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .unread(.enumeration(default: Direction?.none))
    static let format: FieldSchema<ElementFormat> = .unread(.enumeration(default: ElementFormat.empty))
    static let indent: FieldSchema<Double> = .unread(.number(default: 0))
    static let `open`: FieldSchema<JSONValue> = .raw
    static let textFormat: FieldSchema<Double> = .unread(.number(default: 0))
    static let textStyle: FieldSchema<String> = .unread(.string(default: ""))
  }
}

public struct SerializedCollapsibleContentNode: ParentNodePayload {
  public static let type = "collapsible-content"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Double?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.putUnlessDefault("textFormat", textFormat, Schema.textFormat)
    fields.putUnlessDefault("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.textFormat = Schema.textFormat.omittingDefault(textFormat)
    node.textStyle = Schema.textStyle.omittingDefault(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .unread(.enumeration(default: Direction?.none))
    static let format: FieldSchema<ElementFormat> = .unread(.enumeration(default: ElementFormat.empty))
    static let indent: FieldSchema<Double> = .unread(.number(default: 0))
    static let textFormat: FieldSchema<Double> = .unread(.number(default: 0))
    static let textStyle: FieldSchema<String> = .unread(.string(default: ""))
  }
}

public struct SerializedCollapsibleTitleNode: ParentNodePayload {
  public static let type = "collapsible-title"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Double?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.putUnlessDefault("textFormat", textFormat, Schema.textFormat)
    fields.putUnlessDefault("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.textFormat = Schema.textFormat.omittingDefault(textFormat)
    node.textStyle = Schema.textStyle.omittingDefault(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .unread(.enumeration(default: Direction?.none))
    static let format: FieldSchema<ElementFormat> = .unread(.enumeration(default: ElementFormat.empty))
    static let indent: FieldSchema<Double> = .unread(.number(default: 0))
    static let textFormat: FieldSchema<Double> = .unread(.number(default: 0))
    static let textStyle: FieldSchema<String> = .unread(.string(default: ""))
  }
}

public struct SerializedCommentNode: ParentNodePayload {
  public static let type = "comment"
  public static let version = 1
  public var children: [SerializedNode]?
  public var comment: Shaped<Comment>?
  public var direction: Nullable<Never>
  public var format: JSONValue?
  public var indent: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    children = fields.takeChildren()
    comment = fields.take("comment", Schema.comment)
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.put("comment", comment, Schema.comment)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.comment = Schema.comment.resolving(comment)
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    return node
  }

  private enum Schema {
    static let comment: FieldSchema<Shaped<Comment>> = .shaped(.object, .raw)
    static let direction: FieldSchema<Never?> = .enumeration(default: Never?.none)
    static let format: FieldSchema<JSONValue> = .raw
    static let indent: FieldSchema<JSONValue> = .raw
  }
}

public struct SerializedEmojiNode: NodePayload {
  public static let type = "emoji"
  public static let version = 1
  public var className: JSONValue?
  public var detail: JSONValue?
  public var format: JSONValue?
  public var mode: TextMode?
  public var style: JSONValue?
  public var text: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    className = fields.take("className", Schema.className)
    detail = fields.take("detail", Schema.detail)
    format = fields.take("format", Schema.format)
    mode = fields.take("mode", Schema.mode)
    style = fields.take("style", Schema.style)
    text = fields.take("text", Schema.text)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("className", className, Schema.className)
    fields.put("detail", detail, Schema.detail)
    fields.put("format", format, Schema.format)
    fields.put("mode", mode, Schema.mode)
    fields.put("style", style, Schema.style)
    fields.put("text", text, Schema.text)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.className = Schema.className.resolving(className)
    node.detail = Schema.detail.resolving(detail)
    node.format = Schema.format.resolving(format)
    node.mode = Schema.mode.resolving(mode)
    node.style = Schema.style.resolving(style)
    node.text = Schema.text.resolving(text)
    return node
  }

  private enum Schema {
    static let className: FieldSchema<JSONValue> = .raw
    static let detail: FieldSchema<JSONValue> = .aliased(.transform(.raw, Transforms.stringAbsent), ["directionless": 1, "unmergeable": 2])
    static let format: FieldSchema<JSONValue> = .aliased(.transform(.raw, Transforms.stringAbsent), ["bold": 1, "capitalize": 1024, "code": 16, "highlight": 128, "italic": 2, "lowercase": 256, "strikethrough": 4, "subscript": 32, "superscript": 64, "underline": 8, "uppercase": 512])
    static let mode: FieldSchema<TextMode> = .enumeration(default: nil)
    static let style: FieldSchema<JSONValue> = .raw
    static let text: FieldSchema<JSONValue> = .rawOr("")
  }
}

public struct SerializedEquationNode: NodePayload {
  public static let type = "equation"
  public static let version = 1
  public var equation: JSONValue?
  public var inline: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    equation = fields.take("equation", Schema.equation)
    inline = fields.take("inline", Schema.inline)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("equation", equation, Schema.equation)
    fields.put("inline", inline, Schema.inline)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.equation = Schema.equation.resolving(equation)
    node.inline = Schema.inline.resolving(inline)
    return node
  }

  private enum Schema {
    static let equation: FieldSchema<JSONValue> = .rawOr("")
    static let inline: FieldSchema<JSONValue> = .rawOr(false)
  }
}

public struct SerializedExcalidrawNode: NodePayload {
  public static let type = "excalidraw"
  public static let version = 1
  public var data: JSONValue?
  public var figure: Shaped<Figure>?
  public var height: JSONValue?
  public var natural: Shaped<NaturalSize>?
  public var width: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    var state = fields.takeState()
    data = fields.take("data", Schema.data)
    figure = state.take("figure", Schema.figure)
    height = fields.take("height", Schema.height)
    natural = state.take("natural", Schema.natural)
    width = fields.take("width", Schema.width)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    var state = fields.takeState()
    fields.put("data", data, Schema.data)
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("height", height, Schema.height)
    state.putUnlessDefault("natural", natural, Schema.natural)
    fields.put("width", width, Schema.width)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.data = Schema.data.resolving(data)
    node.figure = Schema.figure.omittingDefault(figure)
    node.height = Schema.height.resolving(height)
    node.natural = Schema.natural.omittingDefault(natural)
    node.width = Schema.width.resolving(width)
    return node
  }

  private enum Schema {
    static let data: FieldSchema<JSONValue> = .rawOr("[]", nullAsAbsent: true)
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let height: FieldSchema<JSONValue> = .rawOr("inherit", nullAsAbsent: true)
    static let natural: FieldSchema<Shaped<NaturalSize>> = .shaped(.transform(.object, Transforms.naturalSize), .raw)
    static let width: FieldSchema<JSONValue> = .rawOr("inherit", nullAsAbsent: true)
  }
}

public struct SerializedFigmaNode: NodePayload {
  public static let type = "figma"
  public static let version = 1
  public var documentID: JSONValue?
  public var figure: Shaped<Figure>?
  public var format: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    let holdsState = fields.holdsState
    fields.spreadState()
    var state = fields.takeState()
    documentID = fields.take("documentID", Schema.documentID)
    figure = state.take("figure", Schema.figure)
    format = fields.take("format", holdsState ? Schema.formatWithState : Schema.format)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    var state = fields.takeState()
    fields.put("documentID", documentID, Schema.documentID)
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("format", format, Schema.format)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.documentID = Schema.documentID.resolving(documentID)
    node.figure = Schema.figure.omittingDefault(figure)
    node.format = Schema.format.resolving(format)
    return node
  }

  private enum Schema {
    static let documentID: FieldSchema<JSONValue> = .raw
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let format: FieldSchema<JSONValue> = .raw
    static let formatWithState: FieldSchema<JSONValue> = .checked(.enumeration(default: ElementFormat.empty))
  }
}

public struct SerializedFootnoteDefinitionNode: ParentNodePayload {
  public static let type = "footnote-definition"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var label: String?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    label = fields.take("label", Schema.label)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("label", label, Schema.label)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.label = Schema.label.resolving(label)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let label: FieldSchema<String> = .string(default: "")
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedFootnoteReferenceNode: NodePayload {
  public static let type = "footnote-reference"
  public static let version = 1
  public var label: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    label = fields.take("label", Schema.label)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("label", label, Schema.label)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.label = Schema.label.resolving(label)
    return node
  }

  private enum Schema {
    static let label: FieldSchema<JSONValue> = .raw
  }
}

public struct SerializedHashtagNode: NodePayload {
  public static let type = "hashtag"
  public static let version = 1
  public var detail: Double?
  public var format: Double?
  public var mode: TextMode?
  public var style: String?
  public var text: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    detail = fields.take("detail", Schema.detail)
    format = fields.take("format", Schema.format)
    mode = fields.take("mode", Schema.mode)
    style = fields.take("style", Schema.style)
    text = fields.take("text", Schema.text)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("detail", detail, Schema.detail)
    fields.put("format", format, Schema.format)
    fields.put("mode", mode, Schema.mode)
    fields.put("style", style, Schema.style)
    fields.put("text", text, Schema.text)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.detail = Schema.detail.resolving(detail)
    node.format = Schema.format.resolving(format)
    node.mode = Schema.mode.resolving(mode)
    node.style = Schema.style.resolving(style)
    node.text = Schema.text.resolving(text)
    return node
  }

  private enum Schema {
    static let detail: FieldSchema<Double> = .aliased(.number(default: 0), ["directionless": 1, "unmergeable": 2])
    static let format: FieldSchema<Double> = .aliased(.number(default: 0), ["bold": 1, "capitalize": 1024, "code": 16, "highlight": 128, "italic": 2, "lowercase": 256, "strikethrough": 4, "subscript": 32, "superscript": 64, "underline": 8, "uppercase": 512])
    static let mode: FieldSchema<TextMode> = .enumeration(default: TextMode.normal)
    static let style: FieldSchema<String> = .string(default: "")
    static let text: FieldSchema<String> = .string(default: "")
  }
}

public struct SerializedHeadingNode: ParentNodePayload {
  public static let type = "heading"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var tag: HeadingTag?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    tag = fields.take("tag", Schema.tag)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("tag", tag, Schema.tag)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.tag = Schema.tag.resolving(tag)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let tag: FieldSchema<HeadingTag> = .enumeration(default: HeadingTag.h1)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedHorizontalRuleNode: NodePayload {
  public static let type = "horizontalrule"
  public static let version = 1
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    let fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    return fields.json
  }

  public func resolved() -> Self {
    self
  }
}

public struct SerializedImageNode: NodePayload {
  public static let type = "image"
  public static let version = 1
  public var altText: JSONValue?
  public var caption: NestedEditor?
  public var figure: Shaped<Figure>?
  public var height: JSONValue?
  public var maxWidth: JSONValue?
  public var natural: Shaped<NaturalSize>?
  public var showCaption: JSONValue?
  public var src: JSONValue?
  public var width: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    var state = fields.takeState()
    altText = fields.take("altText", Schema.altText)
    caption = fields.take("caption", Schema.caption)
    figure = state.take("figure", Schema.figure)
    height = fields.take("height", Schema.height)
    maxWidth = fields.take("maxWidth", Schema.maxWidth)
    natural = state.take("natural", Schema.natural)
    showCaption = fields.take("showCaption", Schema.showCaption)
    src = fields.take("src", Schema.src)
    width = fields.take("width", Schema.width)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    var state = fields.takeState()
    fields.put("altText", altText, Schema.altText)
    fields.put("caption", caption, Schema.caption)
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("height", height, Schema.height)
    fields.put("maxWidth", maxWidth, Schema.maxWidth)
    state.putUnlessDefault("natural", natural, Schema.natural)
    fields.put("showCaption", showCaption, Schema.showCaption)
    fields.put("src", src, Schema.src)
    fields.put("width", width, Schema.width)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.altText = Schema.altText.resolving(altText)
    node.caption = Schema.caption.resolving(caption)
    node.figure = Schema.figure.omittingDefault(figure)
    node.height = Schema.height.resolving(height)
    node.maxWidth = Schema.maxWidth.resolving(maxWidth)
    node.natural = Schema.natural.omittingDefault(natural)
    node.showCaption = Schema.showCaption.resolving(showCaption)
    node.src = Schema.src.resolving(src)
    node.width = Schema.width.resolving(width)
    return node
  }

  private enum Schema {
    static let altText: FieldSchema<JSONValue> = .raw
    static let caption: FieldSchema<NestedEditor> = .object
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let height: FieldSchema<JSONValue> = .transform(.raw, Transforms.storedSize)
    static let maxWidth: FieldSchema<JSONValue> = .rawOr(500)
    static let natural: FieldSchema<Shaped<NaturalSize>> = .shaped(.transform(.object, Transforms.naturalSize), .raw)
    static let showCaption: FieldSchema<JSONValue> = .transform(.raw, Transforms.falseOrStored)
    static let src: FieldSchema<JSONValue> = .raw
    static let width: FieldSchema<JSONValue> = .transform(.raw, Transforms.storedSize)
  }
}

public struct SerializedInlineImageNode: NodePayload {
  public static let type = "inline-image"
  public static let version = 1
  public var altText: JSONValue?
  public var caption: NestedEditor?
  public var captionsEnabled: JSONValue?
  public var height: JSONValue?
  public var position: JSONValue?
  public var showCaption: JSONValue?
  public var src: JSONValue?
  public var width: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    altText = fields.take("altText", Schema.altText)
    caption = fields.take("caption", Schema.caption)
    captionsEnabled = fields.take("captionsEnabled", Schema.captionsEnabled)
    height = fields.take("height", Schema.height)
    position = fields.take("position", Schema.position)
    showCaption = fields.take("showCaption", Schema.showCaption)
    src = fields.take("src", Schema.src)
    width = fields.take("width", Schema.width)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("altText", altText, Schema.altText)
    fields.put("caption", caption, Schema.caption)
    fields.put("captionsEnabled", captionsEnabled, Schema.captionsEnabled)
    fields.put("height", height, Schema.height)
    fields.put("position", position, Schema.position)
    fields.put("showCaption", showCaption, Schema.showCaption)
    fields.put("src", src, Schema.src)
    fields.put("width", width, Schema.width)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.altText = Schema.altText.resolving(altText)
    node.caption = Schema.caption.resolving(caption)
    node.captionsEnabled = Schema.captionsEnabled.resolving(captionsEnabled)
    node.height = Schema.height.resolving(height)
    node.position = Schema.position.resolving(position)
    node.showCaption = Schema.showCaption.resolving(showCaption)
    node.src = Schema.src.resolving(src)
    node.width = Schema.width.resolving(width)
    return node
  }

  private enum Schema {
    static let altText: FieldSchema<JSONValue> = .raw
    static let caption: FieldSchema<NestedEditor> = .object
    static let captionsEnabled: FieldSchema<JSONValue> = .rawOr(true, nullAsAbsent: true)
    static let height: FieldSchema<JSONValue> = .transform(.raw, Transforms.storedSize)
    static let position: FieldSchema<JSONValue> = .raw
    static let showCaption: FieldSchema<JSONValue> = .transform(.raw, Transforms.falseOrStored)
    static let src: FieldSchema<JSONValue> = .raw
    static let width: FieldSchema<JSONValue> = .transform(.raw, Transforms.storedSize)
  }
}

public struct SerializedKeywordNode: NodePayload {
  public static let type = "keyword"
  public static let version = 1
  public var detail: JSONValue?
  public var format: JSONValue?
  public var mode: TextMode?
  public var style: JSONValue?
  public var text: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    detail = fields.take("detail", Schema.detail)
    format = fields.take("format", Schema.format)
    mode = fields.take("mode", Schema.mode)
    style = fields.take("style", Schema.style)
    text = fields.take("text", Schema.text)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("detail", detail, Schema.detail)
    fields.put("format", format, Schema.format)
    fields.put("mode", mode, Schema.mode)
    fields.put("style", style, Schema.style)
    fields.put("text", text, Schema.text)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.detail = Schema.detail.resolving(detail)
    node.format = Schema.format.resolving(format)
    node.mode = Schema.mode.resolving(mode)
    node.style = Schema.style.resolving(style)
    node.text = Schema.text.resolving(text)
    return node
  }

  private enum Schema {
    static let detail: FieldSchema<JSONValue> = .aliased(.transform(.raw, Transforms.stringAbsent), ["directionless": 1, "unmergeable": 2])
    static let format: FieldSchema<JSONValue> = .aliased(.transform(.raw, Transforms.stringAbsent), ["bold": 1, "capitalize": 1024, "code": 16, "highlight": 128, "italic": 2, "lowercase": 256, "strikethrough": 4, "subscript": 32, "superscript": 64, "underline": 8, "uppercase": 512])
    static let mode: FieldSchema<TextMode> = .enumeration(default: nil)
    static let style: FieldSchema<JSONValue> = .raw
    static let text: FieldSchema<JSONValue> = .rawOr("")
  }
}

public struct SerializedLayoutContainerNode: ParentNodePayload {
  public static let type = "layout-container"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var figure: Shaped<Figure>?
  public var format: ElementFormat?
  public var indent: Int?
  public var templateColumns: JSONValue?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    var state = fields.takeState()
    direction = fields.takeNullable("direction", Schema.direction)
    figure = state.take("figure", Schema.figure)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    templateColumns = fields.take("templateColumns", Schema.templateColumns)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    var state = fields.takeState()
    fields.putNullable("direction", direction, Schema.direction)
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("templateColumns", templateColumns, Schema.templateColumns)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.figure = Schema.figure.omittingDefault(figure)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.templateColumns = Schema.templateColumns.resolving(templateColumns)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let templateColumns: FieldSchema<JSONValue> = .raw
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedLayoutItemNode: ParentNodePayload {
  public static let type = "layout-item"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Double?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.putUnlessDefault("textFormat", textFormat, Schema.textFormat)
    fields.putUnlessDefault("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.textFormat = Schema.textFormat.omittingDefault(textFormat)
    node.textStyle = Schema.textStyle.omittingDefault(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .unread(.enumeration(default: Direction?.none))
    static let format: FieldSchema<ElementFormat> = .unread(.enumeration(default: ElementFormat.empty))
    static let indent: FieldSchema<Double> = .unread(.number(default: 0))
    static let textFormat: FieldSchema<Double> = .unread(.number(default: 0))
    static let textStyle: FieldSchema<String> = .unread(.string(default: ""))
  }
}

public struct SerializedLineBreakNode: NodePayload {
  public static let type = "linebreak"
  public static let version = 1
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    let fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    return fields.json
  }

  public func resolved() -> Self {
    self
  }
}

public struct SerializedLinkNode: ParentNodePayload {
  public static let type = "link"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var rel: Nullable<String>
  public var target: Nullable<String>
  public var textFormat: Double?
  public var textStyle: String?
  public var title: Nullable<String>
  public var url: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    rel = fields.takeNullable("rel", Schema.rel)
    target = fields.takeNullable("target", Schema.target)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    title = fields.takeNullable("title", Schema.title)
    url = fields.take("url", Schema.url)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.putNullable("rel", rel, Schema.rel)
    fields.putNullable("target", target, Schema.target)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    fields.putNullable("title", title, Schema.title)
    fields.put("url", url, Schema.url)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.rel = Schema.rel.resolving(rel)
    node.target = Schema.target.resolving(target)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    node.title = Schema.title.resolving(title)
    node.url = Schema.url.resolving(url)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let rel: FieldSchema<String?> = .nullable(.string(default: ""), defaultAsNull: true)
    static let target: FieldSchema<String?> = .nullable(.string(default: ""), defaultAsNull: true)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
    static let title: FieldSchema<String?> = .nullable(.string(default: ""), defaultAsNull: true)
    static let url: FieldSchema<String> = .string(default: "")
  }
}

public struct SerializedListNode: ParentNodePayload {
  public static let type = "list"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var listType: ListType?
  public var start: Double?
  public var tag: ListTag?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    listType = fields.take("listType", Schema.listType)
    start = fields.take("start", Schema.start)
    tag = fields.take("tag", Schema.tag)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("listType", listType, Schema.listType)
    fields.put("start", start, Schema.start)
    fields.put("tag", tag, Schema.tag)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.listType = Schema.listType.resolving(listType)
    node.start = Schema.start.resolving(start)
    node.tag = Schema.tag.resolving(tag)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let listType: FieldSchema<ListType> = .aliased(.enumeration(default: ListType.number), ["ol": ListType.number, "ul": ListType.bullet])
    static let start: FieldSchema<Double> = .number(default: 1)
    static let tag: FieldSchema<ListTag> = .enumeration(default: ListTag.ul)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedListItemNode: ParentNodePayload {
  public static let type = "listitem"
  public static let version = 1
  public var children: [SerializedNode]?
  public var checked: Bool?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var textFormat: Double?
  public var textStyle: String?
  public var value: Double?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    checked = fields.take("checked", Schema.checked)
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    value = fields.take("value", Schema.value)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.put("checked", checked, Schema.checked)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    fields.put("value", value, Schema.value)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.checked = Schema.checked.resolving(checked)
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    node.value = Schema.value.resolving(value)
    return node
  }

  private enum Schema {
    static let checked: FieldSchema<Bool> = .optional(.boolean(default: false))
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0, max: 128, clamp: true)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
    static let value: FieldSchema<Double> = .number(default: 1)
  }
}

public struct SerializedMarkNode: ParentNodePayload {
  public static let type = "mark"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var ids: [String]?
  public var indent: Int?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    ids = fields.take("ids", Schema.ids)
    indent = fields.take("indent", Schema.indent)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("ids", ids, Schema.ids)
    fields.put("indent", indent, Schema.indent)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.ids = Schema.ids.resolving(ids)
    node.indent = Schema.indent.resolving(indent)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let ids: FieldSchema<[String]> = .array(.string(default: ""))
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedMentionNode: NodePayload {
  public static let type = "mention"
  public static let version = 1
  public var detail: JSONValue?
  public var format: JSONValue?
  public var mentionName: JSONValue?
  public var mode: TextMode?
  public var style: JSONValue?
  public var text: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    detail = fields.take("detail", Schema.detail)
    format = fields.take("format", Schema.format)
    mentionName = fields.take("mentionName", Schema.mentionName)
    mode = fields.take("mode", Schema.mode)
    style = fields.take("style", Schema.style)
    text = fields.take("text", Schema.text)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("detail", detail, Schema.detail)
    fields.put("format", format, Schema.format)
    fields.put("mentionName", mentionName, Schema.mentionName)
    fields.put("mode", mode, Schema.mode)
    fields.put("style", style, Schema.style)
    fields.put("text", text, Schema.text)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.detail = Schema.detail.resolving(detail)
    node.format = Schema.format.resolving(format)
    node.mentionName = Schema.mentionName.resolving(mentionName)
    node.mode = Schema.mode.resolving(mode)
    node.style = Schema.style.resolving(style)
    node.text = Schema.text.resolving(text)
    return node
  }

  private enum Schema {
    static let detail: FieldSchema<JSONValue> = .aliased(.transform(.raw, Transforms.stringAbsent), ["directionless": 1, "unmergeable": 2])
    static let format: FieldSchema<JSONValue> = .aliased(.transform(.raw, Transforms.stringAbsent), ["bold": 1, "capitalize": 1024, "code": 16, "highlight": 128, "italic": 2, "lowercase": 256, "strikethrough": 4, "subscript": 32, "superscript": 64, "underline": 8, "uppercase": 512])
    static let mentionName: FieldSchema<JSONValue> = .raw
    static let mode: FieldSchema<TextMode> = .enumeration(default: nil)
    static let style: FieldSchema<JSONValue> = .raw
    static let text: FieldSchema<JSONValue> = .raw
  }
}

public struct SerializedMermaidNode: NodePayload {
  public static let type = "mermaid"
  public static let version = 1
  public var figure: Shaped<Figure>?
  public var height: JSONValue?
  public var natural: Shaped<NaturalSize>?
  public var schema: JSONValue?
  public var width: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    var state = fields.takeState()
    figure = state.take("figure", Schema.figure)
    height = fields.take("height", Schema.height)
    natural = state.take("natural", Schema.natural)
    schema = fields.take("schema", Schema.schema)
    width = fields.take("width", Schema.width)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    var state = fields.takeState()
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("height", height, Schema.height)
    state.putUnlessDefault("natural", natural, Schema.natural)
    fields.put("schema", schema, Schema.schema)
    fields.put("width", width, Schema.width)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.figure = Schema.figure.omittingDefault(figure)
    node.height = Schema.height.resolving(height)
    node.natural = Schema.natural.omittingDefault(natural)
    node.schema = Schema.schema.resolving(schema)
    node.width = Schema.width.resolving(width)
    return node
  }

  private enum Schema {
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let height: FieldSchema<JSONValue> = .transform(.rawOr("inherit"), Transforms.zeroAsInherit)
    static let natural: FieldSchema<Shaped<NaturalSize>> = .shaped(.transform(.object, Transforms.naturalSize), .raw)
    static let schema: FieldSchema<JSONValue> = .rawOr("graph TD;\n  A[Start] --> B>Stop]")
    static let width: FieldSchema<JSONValue> = .transform(.rawOr("inherit"), Transforms.zeroAsInherit)
  }
}

public struct SerializedPageBreakNode: NodePayload {
  public static let type = "page-break"
  public static let version = 1
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    let fields = try NodeFields(reading: json, as: Self.type)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    let fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    return fields.json
  }

  public func resolved() -> Self {
    self
  }
}

public struct SerializedParagraphNode: ParentNodePayload {
  public static let type = "paragraph"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let textFormat: FieldSchema<Double> = .number(default: 0)
    static let textStyle: FieldSchema<String> = .string(default: "")
  }
}

public struct SerializedPollNode: NodePayload {
  public static let type = "poll"
  public static let version = 1
  public var options: Shaped<[PollOption]>?
  public var question: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    options = fields.take("options", Schema.options)
    question = fields.take("question", Schema.question)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("options", options, Schema.options)
    fields.put("question", question, Schema.question)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.options = Schema.options.resolving(options)
    node.question = Schema.question.resolving(question)
    return node
  }

  private enum Schema {
    static let options: FieldSchema<Shaped<[PollOption]>> = .shaped(.array(.object), .raw)
    static let question: FieldSchema<JSONValue> = .raw
  }
}

public struct SerializedQuoteNode: ParentNodePayload {
  public static let type = "quote"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var shadowRoot: Bool?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    shadowRoot = fields.take("shadowRoot", Schema.shadowRoot)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.putUnlessDefault("shadowRoot", shadowRoot, Schema.shadowRoot)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.shadowRoot = Schema.shadowRoot.omittingDefault(shadowRoot)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let shadowRoot: FieldSchema<Bool> = .boolean(default: false)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedRootNode: ParentNodePayload {
  public static let type = "root"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var indent: Int?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedSlideNode: NodePayload {
  public static let type = "slide-deck"
  public static let version = 1
  public var data: Shaped<SlideDeck>?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    data = fields.take("data", Schema.data)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("data", data, Schema.data)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.data = Schema.data.resolving(data)
    return node
  }

  private enum Schema {
    static let data: FieldSchema<Shaped<SlideDeck>> = .shaped(.object, .rawOr(["currentSlideId": "default-slide-1", "slides": [["elements": [["editorStateJSON": ["root": ["children": [["children": [["detail": 0, "format": 0, "key": "initial-text-content-node", "mode": "normal", "style": "", "text": "", "type": "text", "version": 1]], "direction": "ltr", "format": "", "indent": 0, "key": "1", "textFormat": 0, "textStyle": "", "type": "paragraph", "version": 1]], "direction": "ltr", "format": "", "indent": 0, "key": "root", "type": "root", "version": 1]], "height": 50, "id": "default-box-1", "kind": "box", "width": 300, "x": 50, "y": 50, "zIndex": 0]], "id": "default-slide-1"]]], nullAsAbsent: true))
  }
}

public struct SerializedStickyNode: NodePayload {
  public static let type = "sticky"
  public static let version = 1
  public var caption: NestedEditor?
  public var color: Shaped<StickyNoteColor>?
  public var xOffset: JSONValue?
  public var yOffset: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    caption = fields.take("caption", Schema.caption)
    color = fields.take("color", Schema.color)
    xOffset = fields.take("xOffset", Schema.xOffset)
    yOffset = fields.take("yOffset", Schema.yOffset)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("caption", caption, Schema.caption)
    fields.put("color", color, Schema.color)
    fields.put("xOffset", xOffset, Schema.xOffset)
    fields.put("yOffset", yOffset, Schema.yOffset)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.caption = Schema.caption.resolving(caption)
    node.color = Schema.color.resolving(color)
    node.xOffset = Schema.xOffset.resolving(xOffset)
    node.yOffset = Schema.yOffset.resolving(yOffset)
    return node
  }

  private enum Schema {
    static let caption: FieldSchema<NestedEditor> = .object
    static let color: FieldSchema<Shaped<StickyNoteColor>> = .shaped(.enumeration(default: StickyNoteColor.yellow), .raw)
    static let xOffset: FieldSchema<JSONValue> = .raw
    static let yOffset: FieldSchema<JSONValue> = .raw
  }
}

public struct SerializedTabNode: NodePayload {
  public static let type = "tab"
  public static let version = 1
  public var detail: Double?
  public var format: Double?
  public var mode: TabMode?
  public var style: String?
  public var text: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    detail = fields.take("detail", Schema.detail)
    format = fields.take("format", Schema.format)
    mode = fields.take("mode", Schema.mode)
    style = fields.take("style", Schema.style)
    text = fields.take("text", Schema.text)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("detail", detail, Schema.detail)
    fields.put("format", format, Schema.format)
    fields.put("mode", mode, Schema.mode)
    fields.put("style", style, Schema.style)
    fields.put("text", text, Schema.text)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.detail = Schema.detail.resolving(detail)
    node.format = Schema.format.resolving(format)
    node.mode = Schema.mode.resolving(mode)
    node.style = Schema.style.resolving(style)
    node.text = Schema.text.resolving(text)
    return node
  }

  private enum Schema {
    static let detail: FieldSchema<Double> = .number(default: 2)
    static let format: FieldSchema<Double> = .aliased(.number(default: 0), ["bold": 1, "capitalize": 1024, "code": 16, "highlight": 128, "italic": 2, "lowercase": 256, "strikethrough": 4, "subscript": 32, "superscript": 64, "underline": 8, "uppercase": 512])
    static let mode: FieldSchema<TabMode> = .enumeration(default: TabMode.normal)
    static let style: FieldSchema<String> = .string(default: "")
    static let text: FieldSchema<String> = .string(default: "\t")
  }
}

public struct SerializedTableNode: ParentNodePayload {
  public static let type = "table"
  public static let version = 1
  public var children: [SerializedNode]?
  public var colWidths: [Double]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var frozenColumnCount: Double?
  public var frozenRowCount: Double?
  public var indent: Int?
  public var rowStriping: Bool?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    colWidths = fields.take("colWidths", Schema.colWidths)
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    frozenColumnCount = fields.take("frozenColumnCount", Schema.frozenColumnCount)
    frozenRowCount = fields.take("frozenRowCount", Schema.frozenRowCount)
    indent = fields.take("indent", Schema.indent)
    rowStriping = fields.take("rowStriping", Schema.rowStriping)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.put("colWidths", colWidths, Schema.colWidths)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("frozenColumnCount", frozenColumnCount, Schema.frozenColumnCount)
    fields.put("frozenRowCount", frozenRowCount, Schema.frozenRowCount)
    fields.put("indent", indent, Schema.indent)
    fields.put("rowStriping", rowStriping, Schema.rowStriping)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.colWidths = Schema.colWidths.resolving(colWidths)
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.frozenColumnCount = Schema.frozenColumnCount.resolving(frozenColumnCount)
    node.frozenRowCount = Schema.frozenRowCount.resolving(frozenRowCount)
    node.indent = Schema.indent.resolving(indent)
    node.rowStriping = Schema.rowStriping.resolving(rowStriping)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let colWidths: FieldSchema<[Double]> = .optional(.array(.number(default: 0)))
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let frozenColumnCount: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let frozenRowCount: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let rowStriping: FieldSchema<Bool> = .optional(.boolean(default: false), omitDefault: true)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedTableCellNode: ParentNodePayload {
  public static let type = "tablecell"
  public static let version = 1
  public var children: [SerializedNode]?
  public var backgroundColor: Nullable<String>
  public var colSpan: Int?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var headerState: Double?
  public var indent: Int?
  public var rowSpan: Int?
  public var textFormat: Double?
  public var textStyle: String?
  public var verticalAlign: VerticalAlign?
  public var width: Double?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    backgroundColor = fields.takeNullable("backgroundColor", Schema.backgroundColor)
    colSpan = fields.take("colSpan", Schema.colSpan)
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    headerState = fields.take("headerState", Schema.headerState)
    indent = fields.take("indent", Schema.indent)
    rowSpan = fields.take("rowSpan", Schema.rowSpan)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    verticalAlign = fields.take("verticalAlign", Schema.verticalAlign)
    width = fields.take("width", Schema.width)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("backgroundColor", backgroundColor, Schema.backgroundColor)
    fields.put("colSpan", colSpan, Schema.colSpan)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("headerState", headerState, Schema.headerState)
    fields.put("indent", indent, Schema.indent)
    fields.put("rowSpan", rowSpan, Schema.rowSpan)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    fields.put("verticalAlign", verticalAlign, Schema.verticalAlign)
    fields.put("width", width, Schema.width)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.backgroundColor = Schema.backgroundColor.resolving(backgroundColor)
    node.colSpan = Schema.colSpan.resolving(colSpan)
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.headerState = Schema.headerState.resolving(headerState)
    node.indent = Schema.indent.resolving(indent)
    node.rowSpan = Schema.rowSpan.resolving(rowSpan)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    node.verticalAlign = Schema.verticalAlign.resolving(verticalAlign)
    node.width = Schema.width.resolving(width)
    return node
  }

  private enum Schema {
    static let backgroundColor: FieldSchema<String?> = .nullable(.string(default: ""), defaultAsNull: true)
    static let colSpan: FieldSchema<Int> = .integer(default: 1, min: 1)
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let headerState: FieldSchema<Double> = .number(default: 0)
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let rowSpan: FieldSchema<Int> = .integer(default: 1, min: 1)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
    static let verticalAlign: FieldSchema<VerticalAlign> = .enumeration(default: nil)
    static let width: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
  }
}

public struct SerializedTableRowNode: ParentNodePayload {
  public static let type = "tablerow"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Direction>
  public var format: ElementFormat?
  public var height: Double?
  public var indent: Int?
  public var textFormat: Double?
  public var textStyle: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    height = fields.take("height", Schema.height)
    indent = fields.take("indent", Schema.indent)
    textFormat = fields.take("textFormat", Schema.textFormat)
    textStyle = fields.take("textStyle", Schema.textStyle)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("height", height, Schema.height)
    fields.put("indent", indent, Schema.indent)
    fields.put("textFormat", textFormat, Schema.textFormat)
    fields.put("textStyle", textStyle, Schema.textStyle)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.height = Schema.height.resolving(height)
    node.indent = Schema.indent.resolving(indent)
    node.textFormat = Schema.textFormat.resolving(textFormat)
    node.textStyle = Schema.textStyle.resolving(textStyle)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Direction?> = .enumeration(default: Direction?.none)
    static let format: FieldSchema<ElementFormat> = .enumeration(default: ElementFormat.empty)
    static let height: FieldSchema<Double> = .optional(.number(default: 0))
    static let indent: FieldSchema<Int> = .integer(default: 0, min: 0)
    static let textFormat: FieldSchema<Double> = .optional(.number(default: 0), omitDefault: true)
    static let textStyle: FieldSchema<String> = .optional(.string(default: ""), omitDefault: true)
  }
}

public struct SerializedTextNode: NodePayload {
  public static let type = "text"
  public static let version = 1
  public var detail: Double?
  public var format: Double?
  public var mode: TextMode?
  public var style: String?
  public var text: String?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    detail = fields.take("detail", Schema.detail)
    format = fields.take("format", Schema.format)
    mode = fields.take("mode", Schema.mode)
    style = fields.take("style", Schema.style)
    text = fields.take("text", Schema.text)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.put("detail", detail, Schema.detail)
    fields.put("format", format, Schema.format)
    fields.put("mode", mode, Schema.mode)
    fields.put("style", style, Schema.style)
    fields.put("text", text, Schema.text)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.detail = Schema.detail.resolving(detail)
    node.format = Schema.format.resolving(format)
    node.mode = Schema.mode.resolving(mode)
    node.style = Schema.style.resolving(style)
    node.text = Schema.text.resolving(text)
    return node
  }

  private enum Schema {
    static let detail: FieldSchema<Double> = .aliased(.number(default: 0), ["directionless": 1, "unmergeable": 2])
    static let format: FieldSchema<Double> = .aliased(.number(default: 0), ["bold": 1, "capitalize": 1024, "code": 16, "highlight": 128, "italic": 2, "lowercase": 256, "strikethrough": 4, "subscript": 32, "superscript": 64, "underline": 8, "uppercase": 512])
    static let mode: FieldSchema<TextMode> = .enumeration(default: TextMode.normal)
    static let style: FieldSchema<String> = .string(default: "")
    static let text: FieldSchema<String> = .string(default: "")
  }
}

public struct SerializedThreadNode: ParentNodePayload {
  public static let type = "thread"
  public static let version = 1
  public var children: [SerializedNode]?
  public var direction: Nullable<Never>
  public var format: JSONValue?
  public var indent: JSONValue?
  public var thread: Shaped<CommentThread>?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    children = fields.takeChildren()
    direction = fields.takeNullable("direction", Schema.direction)
    format = fields.take("format", Schema.format)
    indent = fields.take("indent", Schema.indent)
    thread = fields.take("thread", Schema.thread)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    fields.putChildren(children)
    fields.putNullable("direction", direction, Schema.direction)
    fields.put("format", format, Schema.format)
    fields.put("indent", indent, Schema.indent)
    fields.put("thread", thread, Schema.thread)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.direction = Schema.direction.resolving(direction)
    node.format = Schema.format.resolving(format)
    node.indent = Schema.indent.resolving(indent)
    node.thread = Schema.thread.resolving(thread)
    return node
  }

  private enum Schema {
    static let direction: FieldSchema<Never?> = .enumeration(default: Never?.none)
    static let format: FieldSchema<JSONValue> = .raw
    static let indent: FieldSchema<JSONValue> = .raw
    static let thread: FieldSchema<Shaped<CommentThread>> = .shaped(.object, .raw)
  }
}

public struct SerializedTweetNode: NodePayload {
  public static let type = "tweet"
  public static let version = 1
  public var figure: Shaped<Figure>?
  public var format: JSONValue?
  public var id: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    let holdsState = fields.holdsState
    fields.spreadState()
    var state = fields.takeState()
    figure = state.take("figure", Schema.figure)
    format = fields.take("format", holdsState ? Schema.formatWithState : Schema.format)
    id = fields.take("id", Schema.id)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    var state = fields.takeState()
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("format", format, Schema.format)
    fields.put("id", id, Schema.id)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.figure = Schema.figure.omittingDefault(figure)
    node.format = Schema.format.resolving(format)
    node.id = Schema.id.resolving(id)
    return node
  }

  private enum Schema {
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let format: FieldSchema<JSONValue> = .raw
    static let formatWithState: FieldSchema<JSONValue> = .checked(.enumeration(default: ElementFormat.empty))
    static let id: FieldSchema<JSONValue> = .raw
  }
}

public struct SerializedVideoNode: NodePayload {
  public static let type = "video"
  public static let version = 1
  public var caption: JSONValue?
  public var captionsEnabled: JSONValue?
  public var figure: Shaped<Figure>?
  public var height: JSONValue?
  public var showCaption: JSONValue?
  public var src: JSONValue?
  public var width: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    fields.spreadState()
    var state = fields.takeState()
    caption = fields.take("caption", Schema.caption)
    captionsEnabled = fields.take("captionsEnabled", Schema.captionsEnabled)
    figure = state.take("figure", Schema.figure)
    height = fields.take("height", Schema.height)
    showCaption = fields.take("showCaption", Schema.showCaption)
    src = fields.take("src", Schema.src)
    width = fields.take("width", Schema.width)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    var state = fields.takeState()
    fields.put("caption", caption, Schema.caption)
    fields.put("captionsEnabled", captionsEnabled, Schema.captionsEnabled)
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("height", height, Schema.height)
    fields.put("showCaption", showCaption, Schema.showCaption)
    fields.put("src", src, Schema.src)
    fields.put("width", width, Schema.width)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.caption = Schema.caption.resolving(caption)
    node.captionsEnabled = Schema.captionsEnabled.resolving(captionsEnabled)
    node.figure = Schema.figure.omittingDefault(figure)
    node.height = Schema.height.resolving(height)
    node.showCaption = Schema.showCaption.resolving(showCaption)
    node.src = Schema.src.resolving(src)
    node.width = Schema.width.resolving(width)
    return node
  }

  private enum Schema {
    static let caption: FieldSchema<JSONValue> = .transform(.raw, Transforms.videoCaption)
    static let captionsEnabled: FieldSchema<JSONValue> = .transform(.raw, Transforms.falseOrStored)
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let height: FieldSchema<JSONValue> = .transform(.raw, Transforms.storedSize)
    static let showCaption: FieldSchema<JSONValue> = .transform(.raw, Transforms.falseOrStored)
    static let src: FieldSchema<JSONValue> = .raw
    static let width: FieldSchema<JSONValue> = .transform(.raw, Transforms.storedSize)
  }
}

public struct SerializedYouTubeNode: NodePayload {
  public static let type = "youtube"
  public static let version = 1
  public var figure: Shaped<Figure>?
  public var format: JSONValue?
  public var height: JSONValue?
  public var videoID: JSONValue?
  public var width: JSONValue?
  public var unknownFields: [String: JSONValue]

  public init(json: JSONValue) throws {
    var fields = try NodeFields(reading: json, as: Self.type)
    let holdsState = fields.holdsState
    fields.spreadState()
    var state = fields.takeState()
    figure = state.take("figure", Schema.figure)
    format = fields.take("format", holdsState ? Schema.formatWithState : Schema.format)
    height = fields.take("height", Schema.height)
    videoID = fields.take("videoID", Schema.videoID)
    width = fields.take("width", Schema.width)
    fields.putState(state)
    unknownFields = fields.rest
  }

  public var json: JSONValue {
    var fields = NodeFields(writing: Self.type, version: Self.version, over: unknownFields)
    var state = fields.takeState()
    state.putUnlessDefault("figure", figure, Schema.figure)
    fields.put("format", format, Schema.format)
    fields.put("height", height, Schema.height)
    fields.put("videoID", videoID, Schema.videoID)
    fields.put("width", width, Schema.width)
    fields.putState(state)
    return fields.json
  }

  public func resolved() -> Self {
    var node = self
    node.figure = Schema.figure.omittingDefault(figure)
    node.format = Schema.format.resolving(format)
    node.height = Schema.height.resolving(height)
    node.videoID = Schema.videoID.resolving(videoID)
    node.width = Schema.width.resolving(width)
    return node
  }

  private enum Schema {
    static let figure: FieldSchema<Shaped<Figure>> = .shaped(.object, .raw)
    static let format: FieldSchema<JSONValue> = .raw
    static let formatWithState: FieldSchema<JSONValue> = .checked(.enumeration(default: ElementFormat.empty))
    static let height: FieldSchema<JSONValue> = .transform(.raw, Transforms.storedSize)
    static let videoID: FieldSchema<JSONValue> = .raw
    static let width: FieldSchema<JSONValue> = .transform(.raw, Transforms.storedSize)
  }
}
