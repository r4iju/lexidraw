// The generated OpenAPI document on stdout, so a test can assert on it from a
// process the env stubs never touch.
import { openApiDocument } from "~/server/api/openapi";

console.log(JSON.stringify(openApiDocument));
