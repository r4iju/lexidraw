/**
 * @type {import('prettier').Options}
 */
const config = {
  trailingComma: 'all',
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  printWidth: 80,
  endOfLine: 'auto',
};

export default config;
