{
  "name": "@packages/lib",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "bun x tsgo --watch --project tsconfig.json",
    "build": "bun x tsgo --project tsconfig.build.json",
    "lint": "bun x @biomejs/biome check --write .",
    "test:ts": "bun x tsgo --noEmit --project tsconfig.json",
    "clean": "rm tsconfig.tsbuildinfo && rm -rf ./dist && rm -rf .turbo && rm -rf node_modules",
    "test": "bun test"
  },
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./*": [
      "./dist/*.js",
      "./dist/*/index.js"
    ]
  },
  "dependencies": {
    "@packages/env": "workspace:*",
    "@vercel/blob": "^2.8.0",
    "zod": "^4.6.5"
  },
  "typesVersions": {
    "*": {
      "*": [
        "src/*",
        "src/*/index"
      ]
    }
  },
  "publishConfig": {
    "typesVersions": {
      "*": {
        "*": [
          "dist/*.d.ts",
          "dist/*/index.d.ts"
        ]
      }
    }
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "1.3.0",
    "@types/node": "^22.20.4",
    "@typescript/native-preview": "^7.0.0-dev.20260707.2"
  }
}
