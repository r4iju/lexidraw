export enum PublicAccess {
  READ = "READ",
  PRIVATE = "PRIVATE",
  EDIT = "EDIT",
}

export enum AccessLevel {
  READ = "READ",
  EDIT = "EDIT",
}

export enum EntityType {
  DRAWING = "drawing",
  DOCUMENT = "document",
  DIRECTORY = "directory",
}

export enum Theme {
  LIGHT = "light",
  DARK = "dark",
}
