/**
 * The built document, as the `resources/read` answers with it: the Excalidraw
 * editor, the MCP Apps bridge, and the stylesheet in one self-contained HTML
 * file, with nothing left to fetch but the fonts `DRAWING_PREVIEW_CSP` allows.
 *
 * The value is written by `scripts/build.ts`; this file is only the type
 * surface, so a type check needs no build.
 */
export declare const DRAWING_PREVIEW_HTML: string;
